# 多 Agent 协作销售分析系统

## 目标
从空仓库实现可运行的前后端分离销售分析系统，覆盖权限、商品、Excel 导入、门店库存、销售分析、冷热评分和固定多 Agent 工作流。

## 阶段

| 阶段 | 状态 | 验证标准 |
|---|---|---|
| P0. 完整计划与接口评审 | in_progress | 先定稿项目计划，再生成并评审各业务模块独立接口文档 |
| P1. 样表与数据模型 | pending | 基于脱敏样表确认字段、类型、约束、关系和映射 |
| P2. 可运行工程骨架 | pending | 健康接口、系统状态页和浏览器联调通过 |
| P3. 登录、用户和门店 | pending | 管理员/普通用户权限页面测试通过 |
| P4. 商品与基础资料 | pending | 两种角色商品 DTO 和页面验证通过 |
| P5. Excel 映射与预检 | pending | xls/xlsx 映射、预检、幂等和错误阻断通过 |
| P6. 商品资料同步与 POP 库存 | pending | 分批同步、库存覆盖、快照、同步记录、汇总和告警通过 |
| P7. 每日销售导入 | pending | 销售事实、部分库存覆盖、乱序保护和门店隔离通过 |
| P8. 经营看板 | pending | 已确认指标接口和图表页面通过 |
| P9. 冷热商品 | pending | 配置、评分快照和解释页面通过 |
| P10. 固定多 Agent | pending | 工作流、SSE、图表和报告页面通过 |
| P11. 审计、验收与上线 | pending | 全流程页面验收和部署检查通过 |

## 实施约束
- 模块化单体，不引入微服务或消息队列；商品搜索按用户要求使用 Elasticsearch。
- 每个后端模块同步提供前端页面与 API 客户端。
- 普通用户能查看所属门店的商品公开字段和最新已知库存，并能为所属门店导入每日销售汇总；不能查看其他门店库存、查询经营数据或访问 AI。
- 同一商品的当前售价按门店分别保存；普通用户只能查看所属门店售价，管理员可查看并维护各门店售价。
- 每日销售表中的“当前机构售价”用于更新目标门店售价；营业日期不早于现有售价来源日期时才允许覆盖，旧日期导入只保留销售快照，不回退当前售价。
- 商品资料导入前由管理员必选资料日期，代表 POP 导出数据日期；该日期作为批次内进价、售价和库存的来源日期，旧资料不得回退较新的当前值。
- 管理员手工修改门店售价时只提交售价和并发版本，不填写日期；系统自动以操作当天作为售价来源日期，并记录精确操作时间。
- 每日销售事实长期保留且首版不自动删除；用户选择的主查询区间最多连续 31 个自然日，同比、环比由后端自动读取对应对比区间。
- 当前进价按“门店 + 商品 + 供应商”唯一保存，每个组合只保留最新进价和来源日期，不建设历次进价历史；商品资料和每日销售汇总只更新目标门店对应组合，不能影响其他门店，历史销售成本仍只使用每日销售事实中的成本快照，普通用户不能查看进价。
- 业务审计日志只记录关键操作的操作人、时间、对象、结果和必要变更摘要，不是销售流水或程序错误日志；仅管理员可查，供应商及报价变化不进入审计。
- 商品资料中的毛利率舍弃且不作为导入目标字段；历史毛利分析只使用每日销售事实中的销售毛利率快照。
- 每日销售表中的同期销售收入、同期销售数量和同期销售毛利率全部舍弃；同比取去年对应日期区间，环比取紧邻主查询区间之前的等长区间，历史不足时明确返回暂无可比数据。
- 每日销售表中“本期销售数量”等于 0 的行整行舍弃，不创建销售事实，也不更新当前进价、门店售价或库存；即使销售收入非 0 也执行此规则。负销量不属于零销量忽略行，仍作为退货事实处理。
- 业务方保证每日销售文件过滤零销量行后至少存在一条非零销量数据；首版不为“整份文件均被过滤”设计专用错误码、提交分支或验收场景。
- 每日销售导入前必须选择营业日期；普通用户门店取账号绑定值，不能由客户端指定。
- 普通用户可查看所属门店全部销售导入批次，但只能提交或放弃本人创建的未提交批次。
- POP 中的商品和库存数据完整且是唯一权威来源；新系统可通过多个商品资料批次逐步同步，每批与销售文件一样只覆盖出现的条码。“尚未同步到本系统”不表示商品尚未进入 POP 或尚未完成实体入库。
- 销量、退货和销售修正不再加减库存；不建设进货累加或人工库存调整逻辑。
- 总库最新已知库存为所有门店商品各自最后一次 POP 库存的查询汇总，不进行双写，并必须展示数据新鲜度。
- Excel 存在错误行时整批不可提交。
- 商品资料更新采用非空覆盖：已有条码的空单元格保留系统旧值；新商品缺少必填字段、条码为空或非空值格式错误时仍整批不可提交。
- 商品条形码按字符串处理，格式为 1 至 64 位数字或英文字母并保留前导零；允许管理员通过商品编辑功能受控修改，普通用户只读。修改时内部商品身份和历史业务关系保持不变，不建设旧条码别名，所有导入只按当前唯一条码匹配。
- 商品单位直接保存为自由文本，不建设单位字典；管理员可通过商品维护功能修改，普通用户只读。
- 商品分类全系统共享，不按门店隔离，最多 5 级且根分类计为第 1 级；普通用户和管理员均可创建父级分类、调整类别的父级归属。商品只能直接关联叶子分类，分类不能同时直接挂商品和拥有子分类。管理员可在商品页面把商品调整至其他启用的叶子分类，普通用户只读，变更写入审计。创建或移动后超过 5 级，或者目标父分类已有商品时必须拒绝；移动操作同时校验整个子树深度。
- POP 样表只有品类编码和品类名称，不能推断父级；POP 品类编码作为全系统唯一匹配键。条码首次进入本系统时，商品资料中的 POP 类别用于初始化该商品分类；新 POP 类别首次出现时创建为根分类。已有商品后续无论导入商品资料还是每日销售汇总，即使同一条码携带的品类发生变化，也以首次导入后保存在系统中的当前分类为准，不由导入覆盖；管理员仍可在系统中修改商品分类。已有分类的名称和父级也不由导入覆盖，统一在页面维护。人工父级只填写名称、不设置业务编码，由系统内部 ID 标识。普通用户不可重命名或停用分类，这两项操作仅管理员可执行。
- 管理员可停用已有商品关联的系统分类，停用不级联修改商品状态或分类归属；商品继续可查询并显示“分类已停用”。停用分类不得用于新建商品或手工改分类。POP 源端不会停用分类，导入不设计“新商品命中已停用 POP 分类”的阻断或自动处理分支。
- 首版只提供【商品资料】和【每日销售汇总】两种导入模板，不再把商品资料拆分为“只更新资料”和“资料+库存同步”两个业务模式；两类导入均可按来源日期更新库存、当前进价和门店售价。
- “第一次初始化导入”按每个商品条码首次进入本系统判定，不按全系统首个批次判定；因此后续小批量导入的新商品仍可使用 POP 类别初始化，已有商品的分类永不被导入覆盖。
- 在真实样表和字段清单确认前，不创建数据库业务表或 Flyway 正式迁移。
- P0 未通过前冻结新增业务代码、数据库迁移和功能页面。
- 项目计划先单独定稿；定稿后按 M01–M10 拆分业务模块，每个模块生成独立接口文档并评审，通过后才进入数据模型和数据库阶段。

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 暂无 | 0 | - |
| Maven 无法写入系统级仓库 | 1 | 改用工作区内 `backend/.m2/repository` |
| npm 无法写入用户级缓存 | 1 | 改用工作区内 `frontend/.npm-cache` |
| 同一补丁不能删除并新建同一路径 | 1 | 将文档替换拆成删除、创建两步 |
| 库存方案批量补丁上下文不匹配 | 2 | 停止跨文件批量补丁，改为逐文件更新 |
| README 引用的 `backend/pom.xml` 不存在 | 1 | 确认未评审后端草案已删除，README 改为准确说明当前仅处于计划评审阶段 |
| README 同路径删除并新建补丁失败 | 2 | 按工具约束拆分为删除和新建两次补丁 |
| GitHub CLI 已保存令牌失效 | 1 | 不把令牌写入仓库或远程 URL；使用临时认证完成本次推送 |
| Git fetch 无法写入 `.git/FETCH_HEAD` | 1 | 工作区对 `.git` 仅开放读取；后续 Git 暂存、提交、拉取和推送使用明确授权的提升权限 |
| GitHub HTTPS fetch/push 连接被重置或超时 | 5 | HTTP 浅拉取曾成功并完成历史合并；最终按用户要求切换到已验证的 SSH 连接推送 |
| 沙箱无法直接列出用户 `.ssh` 目录 | 1 | 使用明确授权的只读提升权限确认现有密钥，不读取私钥内容 |
| 供应商与审计规则跨文档补丁上下文不匹配 | 1 | 未产生部分修改；改为按文件分组应用小补丁 |
| PowerShell 校验命令中的 Markdown 反引号触发 Unicode 转义解析错误 | 1 | 改用单引号包裹 `rg` 搜索表达式后验证通过 |
| 多级分类跨文档补丁因 CONTEXT 上下文不匹配而失败 | 1 | 未产生部分修改；读取实际段落后改为分组补丁 |
| 误触发无效的协作工具调用 | 1 | 调用参数校验失败且未创建任何子任务；继续由主任务处理 |
| 分类导入规则跨文档补丁上下文不匹配 | 1 | 未产生部分修改；读取实际段落后拆分补丁 |
| 分类 API 三段合并补丁上下文校验失败 | 1 | 预览、导入规则和审计规则改为逐段应用并成功 |
| 手工分类优先级跨文档补丁上下文不匹配 | 1 | 未产生部分修改；读取 API-030/API-031 实际路径后拆分更新 |
| 手工分类优先级 API 合并补丁上下文不匹配 | 1 | 未产生部分修改；按商品视图、导入规则和预检响应逐段更新 |
| 恢复 POP 分类跨文档补丁上下文不匹配 | 1 | 未产生部分修改；读取 Product 模型实际描述后分组更新 |
| 恢复 POP 分类 API 补丁匹配文本误写 | 1 | 未产生部分修改；移除重复筛选字段并改为小段补丁 |
| 恢复分类审计补丁重复应用 | 1 | 目标文本已在前序补丁中完成；核对实际内容后只补充 API-120 摘要 |
| 最新规则的计划台账批量补丁上下文不匹配 | 1 | 未产生部分修改；按文件实际结尾拆分补丁并补充记录 |
| 粒度校验把 `StoreProductSupplier` 误判为旧 `ProductSupplier` | 1 | 将检查改为仅匹配不带 `Store` 前缀的旧实体名后重新验证 |
