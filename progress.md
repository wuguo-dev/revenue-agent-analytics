# 实施进度

- 2026-09-13：创建 M03 独立逐步确认稿，登记 9 个剩余步骤；当前只开放第 1 步“商品创建来源”。
- 2026-09-13：M03 第 1 步确认方案 A，商品只能由 POP 商品资料创建；人工新增接口 API-034 待第 2 步范围确认后一并从总契约移除。第 2 步已开放，用于厘清 POP 覆盖范围与既有分类、条码和仓库货位规则的冲突。
- 2026-09-13：M03 第 2 步主体确认方案 A，仅 POP 实际来源字段按条形码覆盖；分类及系统维护的分类树、仓库、货位保留既定例外。当前只待确认管理员纠正错误条形码的能力，第 2 步尚未关闭。
- 2026-09-13：用户确认不需要管理员纠正条形码，只保留管理员删除商品资料能力；M03 第 2 步已关闭，第 3 步改为商品删除生命周期并开放首个问题：已有历史引用时允许逻辑删除并保留历史，还是禁止删除。
- 2026-09-13：用户否决逻辑删除和引用阻断，确认商品主档直接物理删除且不保留历史身份。已记录其对现有历史数据基线的影响，第 3 步下一项只确认是否级联永久删除全部商品关联数据。
- 2026-09-13：用户确认物理删除商品时一并永久删除全部商品关联的当前数据、历史数据、分析结果、搜索索引及既有审计记录。已将该规则定义为历史保留基线的例外；第 3 步继续确认多商品共享导入批次的处理方式。
- 2026-09-13：用户确认共享导入批次采用方案 A，仅删除目标商品明细并重算批次统计，保留批次和其他商品。第 3 步继续确认删除操作自身是否保留不含商品身份信息的审计记录。
- 2026-09-13：用户确认删除操作审计采用方案 B，不保留任何删除审计。第 3 步继续确认被删商品的同一条码再次从 POP 导入时，是作为全新商品创建还是永久禁止。
- 2026-09-14：用户确认同条码再次导入采用方案 A，按全新商品创建且不恢复旧数据；M03 第 3 步关闭，第 4 步开放，当前只确认是否建设独立供应商主档。
- 2026-09-15：用户确认 M03 第 4 步供应商模型采用方案 A，不建设供应商主档或供应商 ID，当前来源只保存名称文本；继续确认供应商名称清洗规则。
- 2026-09-15：用户确认供应商名称清洗采用方案 A，首尾空白去除、清洗后为空保留旧值且内部字符不归一。第 4 步继续确认普通用户与管理员的供应来源 DTO 字段。
- 2026-09-15：用户确认供应来源 DTO 采用方案 A；M03 第 4 步关闭，第 5 步开放，当前只确认分类同级排序是否支持人工 `sortOrder`。
- 2026-09-15：用户确认分类排序采用方案 A，支持人工 `sortOrder` 并以名称、ID 作为稳定后备排序；第 5 步继续确认分类树 DTO 是否对两种角色返回相同字段。

## 2026-09-07
- 读取并采用文件化规划与精简编码约束。
- 确认仓库为空，开始阶段 1。
- 创建项目文档、Docker Compose、数据库基线、后端通用响应/安全骨架和 Vue 应用外壳。
- 首次 Maven/npm 验证因系统级缓存目录无写权限失败；改用项目内临时缓存继续验证。
- 发现数据库字段尚未完成业务确认，已删除提前创建的 Flyway 基线草案；没有启动或修改任何数据库。
- 详细计划首次替换因补丁工具不允许同路径删除后重建而失败；改为分步执行。
- 用户要求先确认完整计划，并将粒度细化到具体接口。
- 已生成 P0–P11 实施计划评审稿和 API-001 至 API-122 接口契约评审稿；未继续业务开发。
- 复核并修正旧计划对 Elasticsearch 的遗漏，增加商品索引同步、状态和全量重建接口。
- 收到商品资料和商品销售汇总两份 `.xls`，开始只读字段评审；尚未创建数据库或业务实现。
- 完成首轮结构与质量剖析：确认销售表汇总行、重复条码/多供应商、派生字段、售价与收入差异及缺失营业日期等关键问题。
- 根据用户反馈确认多供应商与销售聚合规则，新增 `docs/source-field-review.md` 逐字段评审稿；未创建数据库。
- 确认管理员和普通用户均可导入每日销售汇总，营业日期在导入前由页面必选；已同步更新计划、权限矩阵和 API 契约，仍未创建数据库或业务实现。
- 确认普通用户可查看所属门店全部销售导入批次；接口契约按“门店内可读、本人批次可操作”更新，仍未创建数据库或业务实现。
- 完成 POP 作为唯一库存来源的业务评估，输出 `docs/inventory-source-assessment.md`；当前仅为 PROPOSED，尚未改动正式计划、API 契约、数据模型或数据库。
- 用户正式确认 POP 权威库存与每日销售部分快照规则；已将评估稿改为 ACCEPTED，并同步正式计划、API 契约、字段评审、数据字典、领域词汇和 ADR。未创建数据库或实现业务代码。
- 补充确认商品资料表可用于周期性覆盖旧库存，形成“商品资料批量刷新 + 每日销售局部刷新”的双入口；同时确认普通用户可查看所属门店库存。相关计划和接口已同步，数据库仍未创建。

## 2026-09-09

- 确认商品资料表是单门店全量商品快照，包含零库存和已停用商品；已同步库存方案、领域术语、ADR、实施计划和接口清单。
- 确认文档交付顺序为“先定稿项目计划，再拆分 M01–M10 业务模块并分别生成接口文档”；尚未生成模块接口文件，也未创建数据库。
- 澄清商品资料“可导出全量”不等于“单批次强制全量”：系统支持多次分批导入，每批只新增/覆盖出现条码，未出现商品不处理；已移除全量缺失阻断设计。
- 进一步确认商品资料小批次增量 upsert：可在已有商品主档上新增未录入系统的新货品，已有条码按规则更新，批次外商品保持不变。
- 消除“新货未入库”的术语歧义：POP 中商品及库存数据已经完整，“未入库”实际指尚未同步到当前规划的新系统；已将计划、接口清单、库存方案、字段评审、数据字典、领域词汇和 ADR 统一改为“尚未同步到本系统”。
- 确认商品资料已有条码的空字段保留系统旧值；已将“非空覆盖、新商品必填字段仍校验、空值不代表清空”写入实施计划、接口清单、库存方案、字段评审和数据字典，并从接口未决项中移除。
- 确认商品条形码仅管理员可修改、普通员工只读；已更新领域术语、计划、API-035、权限验收、字段评审、数据字典和审计范围。旧条码修改后的导入匹配行为保留为下一项待确认。
- 确认不建设历史条码别名：条码修改后只保留当前条码参与唯一校验、查询和导入匹配；旧值仅存在于审计记录。已关闭该待确认项并同步相关计划文档。

## 2026-09-10

- 确认条码按字符串保存、保留前导零、允许数字和英文字母且最大 64 位；已补充格式错误码与边界验收，并从接口评审待确认项中移除条码格式。

## 2026-09-11

- 用户要求将当前项目文件推送到指定 GitHub 仓库并同步更新 README。
- 已确认本地 `origin` 指向目标仓库，当前分支为 `main`，GitHub CLI 已保存凭据失效。
- 已完成项目密钥模式扫描，未发现待提交文件包含实际访问令牌或 API Key。
- 已重写 README，准确标明项目处于 P0 计划评审阶段、后端尚未生成、当前不可部署，并汇总已确认业务规则、文档导航和后续门禁。
- 首次临时认证拉取未写入任何凭据，但因沙箱禁止写入 `.git/FETCH_HEAD` 失败；需要提升 Git 操作权限后继续，且不会强制覆盖远程分支。
- 已创建本地提交 `25271fc`；提升权限后的首次 GitHub 拉取因连接被重置失败，远程内容尚未改变，准备改用 HTTP/1.1 重试。
- 仓库改为 Public 后已匿名取得远程 `main`；使用 HTTP/1.1 浅拉取成功。远程只有独立的 README 初始提交，与本地历史无共同祖先。
- 已使用允许无关历史的普通合并保留远程提交；README 的 add/add 冲突已按双方意图解决，以详细规划说明为主体，并保留日报、周报和库存警戒作为待评审愿景。
- HTTPS 推送在对象写出后再次断开，结果无法可靠确认；根据用户指示改用 SSH，本机密钥成功认证 GitHub 账号 `wuguo-dev`。
- 已将 `origin` 切换为 SSH 地址并成功推送合并提交 `847cf1b`，远程 `main` 从 `3cb38f5` 前进到 `847cf1b`，本地 `main` 已设置跟踪 `origin/main`。
- 确认同一商品当前售价按门店分别保存：普通用户只查看本店售价，管理员可查看并维护各门店售价；已同步领域词汇、总体计划、接口清单、字段评审、数据字典和 README，未创建数据库或业务代码。

## 2026-09-12

- 开始 M01“系统与认证”模块定稿工作；将先盘点已有接口、角色权限、安全边界和上下游依赖，再按逐步确认规则向用户集中提出需拍板事项。确认前不创建数据库、迁移或业务代码。
- 完成 M01 现状、安全基线和模块 seam 梳理；创建 `docs/api/01-system-auth.md` 完整评审草案并拆为 8 个逐步确认主题。当前只进入第 1 步“模块目标、范围、职责与 seam”，其余推荐参数仍未确认。
- M01 草案结构校验通过：API 编号为 001、002、009～012，代码围栏成对，文档与第 1 步状态均为待逐步确认；未创建数据库、迁移、Redis 键或业务代码。
- 用户明确确认 M01 第 1 步“模块目标、范围、职责与 seam”；已记录 M01/M02/M10 职责和内部 adapter seam，当前进入第 2 步“会话模型、Cookie/CSRF 与有效期”。
- 用户明确确认 M01 第 2 步“会话模型、Cookie/CSRF 与有效期”；已记录 Redis 服务端会话、HttpOnly Cookie、CSRF/Origin 校验、30 分钟空闲与 12 小时绝对有效期，当前进入第 3 步。
- 第 3 步发起确认前复核 OWASP 密码存储与认证指南：将新密码哈希从 bcrypt 修正为 Argon2id，避免 bcrypt 72 字节边界与 Unicode 规则冲突；无 MFA 场景的密码长度改为 15～128 字符，并补充本地常见密码拒绝清单。
- 用户说明账号统一由管理员创建并分发给员工，登录校验和密码安全无需复杂；第 3 步已据此简化为 bcrypt、8～32 位基础密码、无复杂组合/弱密码库/周期改密/首次强制改密，仅保留统一失败响应、5 次失败冷却和安全哈希底线，继续等待第 3 步确认。
- 用户进一步明确员工没有修改密码权限；已从 M01 删除员工主动改密规则，并同步从 M02 草案删除 API-019、个人密码 DTO、错误码、页面、权限和实施任务。密码创建与重置仅由管理员执行，第 3 步继续等待确认。
- 用户明确确认修改后的 M01 第 3 步：账号由管理员统一创建、分发和重置，员工没有改密权限；采用 bcrypt、8～32 位半角密码、统一失败响应和简单失败冷却。当前进入第 4 步会话失效与并发登录评审。
- 用户明确确认 M01 第 4 步：同一账号最多 3 个会话，第 4 次登录撤销最早会话；退出只撤销当前会话，管理员重置密码、取消账号登录资格、修改角色或门店后全部旧会话失效，Redis 不可用时安全失败。当前进入第 5 步。
- 为保持固定两角色模型简单，第 5 步权限集合从 12 个细分 code 收敛为现有 `SYSTEM_READ`、`USER_CATALOG`、`CATEGORY_ORGANIZE`、`SALES_IMPORT` 和 `ADMIN`；前端静态维护菜单结构，后端继续独立鉴权和限制门店范围。
- 用户明确确认 M01 第 5 步：`ADMIN/USER` 固定角色，后端返回 5 个稳定权限 code，前端静态维护菜单结构并按 code 裁剪，业务模块继续强制鉴权和门店范围。当前进入第 6 步接口契约评审。
- 第 6 步发起确认前修正退出接口矛盾：API-011 改为 `PUBLIC + CSRF`，有会话时撤销、无会话时仍幂等清 Cookie；同时补充登录传输字段约束和 `/auth/me` 滑动续期/失效 Cookie 行为。
- 用户在了解 API-001 是供 Docker、负载均衡和运维监控使用的机器健康探针后，明确确认 M01 第 6 步；健康标准固定为后端进程、MySQL 和 Redis，Elasticsearch/AI 异常不把整体状态判为 DOWN。当前进入第 7 步。
- 用户明确确认 M01 第 7 步：采用已列出的认证错误码、登录/退出/会话撤销审计范围、敏感字段排除、HTTPS/no-store/安全响应头和默认同站点策略。当前进入最后的第 8 步。
- 第 8 步发起确认前修正 HttpOnly Cookie 下的退出失败处理：网络失败时前端无法自行删除会话 Cookie，因此保留当前页面并提示重试，只有服务端确认退出后才清空主体和广播退出。

- 开始 M05 POP 库存独立粒度规划；已在总计划登记 P0-M05 子阶段，并确认本轮只补充文档和用户确认，不创建数据库、迁移或业务实现。
- 对照总体计划、总接口清单、数据字典、库存 ADR、库存来源评估和领域术语完成现状检查；发现需要拆清当前投影、批次级同步摘要、商品级观察快照、新鲜度计算和告警生命周期。
- 按深模块原则确定初步 seam：M04/M06 只向 M05 提交标准化库存观察命令，M05 独立负责时序判断、并发保护、快照、当前投影、同步摘要和告警；前端不编排这些内部步骤。
- 已创建 `docs/api/05-inventory.md` 确认稿，覆盖数据粒度、M04 adapter seam、写入顺序、API-080 至 API-089、视图、权限、错误码、页面和验收矩阵；文档保持 `PENDING_CONFIRMATION`。
- 完成静态一致性检查：接口编号无重复，旧日期跳过不刷新当前投影，新鲜度与来源日期分离，普通用户只经 M03 读取本店库存；尚未把待确认默认值同步进总接口契约。
- 用户确认 M05 粒度方案，并要求陈旧库存扫描改为每天凌晨 04:00（Asia/Shanghai）。已将 M05 文档改为 `APPROVED`，完成总接口契约、总体计划、数据字典、领域术语、库存方案和 README 的一致性同步，P0-M05 标记完成；未创建数据库或业务代码。
- 开始 M04 导入中心独立粒度规划；已确认目标文件尚未创建，并在总计划中登记 P0-M04 子阶段。当前仅规划接口与验收，不创建数据库、迁移、后端或前端业务代码。
- 完成 M04 现状差距梳理：需补齐上传解析与预检分阶段、模板启停与映射快照、状态机、权限矩阵、行级结果、提交并发幂等、错误文件脱敏及前后端验收任务。
- 按深模块原则确定 M04 seam：导入中心拥有通用工作流与状态机，具体商品、库存、销售变更通过 M03/M05/M06 端口执行；后续文档会把这些依赖和测试替身写明。
- 已创建 `docs/api/04-imports.md`，细化 API-060 至 API-075、目标字段、预检顺序、状态机、幂等/并发、错误 Excel、页面流程、18 项前后端任务和验收矩阵；同步更新总接口稿、总体计划、库存方案和 README 导航。
- 首轮静态检查确认 M04 API-060 至 API-075 连续且无缺号，`git diff --check` 未报格式错误；本地 Markdown 链接脚本未处理 README 的空父路径，已调整校验方案。
- M04 最终静态检查通过：16 个接口编号与总接口稿完全一致、无重复接口行、Markdown 链接有效、旧有效路径无残留；P0-M04 已标记完成，整体 P0 及并行的 P0-M05 状态保持不变。
- 用户新增逐步确认要求；已纠正上一条状态，将 P0-M04 退回 `in_progress`，在 M04 文档新增 10 步确认清单，并将该门禁同步到总计划和 README。当前仅等待确认第 1 步，不会自动进入后续步骤。
- 用户确认 M04 第 1 步通过；已记录确认日期，并将第 2 步“角色权限与门店数据范围”设为当前待确认项。
- 用户确认 M04 第 2 步通过；已记录权限及门店数据范围，并将第 3 步“批次枚举与状态机”设为当前待确认项。
- 用户确认 M04 第 3 步通过；已记录批次状态、转换和失败回滚规则，并将第 4 步“模板生命周期与目标字段”设为当前待确认项。
- 用户确认 M04 第 4 步通过；已记录模板生命周期、两类目标字段及固定忽略列，并将第 5 步“字段映射优先级与映射校验”设为当前待确认项。
- 用户确认 M04 第 5 步通过；按解释后的简化方案收紧权限为“普通用户免配置、仅管理员可手工映射”，并将第 6 步“文件上传、解析限制与预检顺序”设为当前待确认项。
- 用户询问第 6 步中的实际表头；工作区没有原始 Excel 可复核，已明确区分“已记录的规范化字段名”和“原始 Excel 的实际表头行布局”，第 6 步保持待确认。
- 用户随后确认 M04 第 6 步通过；已记录文件限制、规范化表头和预检顺序，将原始表头行布局列为编码前复核项，并把第 7 步接口契约设为当前待确认项。
- 用户在澄清 API-069 含义后确认 M04 第 7 步通过；已统一接口描述，并将第 8 步“两类导入业务预检规则”设为当前待确认项。
- 确认每日销售表“当前机构”指目标门店，“当前机构售价”在营业日期不早于现有售价来源日期时覆盖该店当前售价；较旧日期仅保存销售快照。已同步领域词汇、总体计划、接口清单、字段评审、数据字典和 README，未创建数据库或业务代码。
- 确认商品资料导入由管理员必选资料日期，作为售价和库存来源日期并阻止旧资料回退现值；已同步总体计划、API-067、库存接口、字段评审、数据字典、库存方案、ADR 和 README，未创建数据库或业务代码。
- 确认管理员手工修改门店售价时无需填写日期，系统自动使用操作当天并记录精确时间；已同步 API-039、权限验收、审计范围、领域词汇、字段评审、数据字典和 README，未创建数据库或业务代码。
- 确认每日销售事实长期保留、首版不自动删除，31 天仅为单次主查询窗口；同比和环比由后端自动读取对比区间。已同步总体计划、接口校验、字段评审、数据字典、领域词汇和 README，未创建数据库或业务代码。
- 确认商品—供应商只保存最新进价，不保存进价历史；同时补充业务审计日志的定义、范围、权限、字段和敏感信息排除规则。已同步总体计划、API-037/038/120、字段评审、数据字典、领域词汇和 README，未创建数据库或业务代码。
- 确认商品资料毛利率舍弃、每日销售毛利率作为历史事实快照保留；已同步导入目标字段、预检忽略列、总体计划、API 清单、字段评审、数据字典、领域词汇和 README，未创建数据库或业务代码。
- 确认舍弃每日销售表三列同期值，并固定系统同比/环比区间与历史不足返回规则；已同步导入目标白名单、预检忽略列、分析接口、字段评审、数据字典、领域词汇和 README，未创建数据库或业务代码。
- 确认每日销售表“本期销售数量 = 0”的行整行舍弃，不创建销售事实且不更新售价或库存，负销量仍作为退货事实；已同步预检统计、行结果类型、总体计划、字段评审、库存方案、数据字典、领域词汇和 README，未创建数据库或业务代码。
- 确认不会出现每日销售文件全部行均因零销量被过滤的情况；已将其记录为业务前提，并明确首版不新增专用错误码、空批次分支或对应验收场景，未创建数据库或业务代码。
- 确认商品单位采用自由文本、不建立单位字典，管理员可修改、普通用户只读；已同步总体计划、商品接口、字段评审、数据字典和 README，未创建数据库或业务代码。
- 将商品分类方案从一级改为全系统共享的多级父子分类；确认商品资料带入类别，普通用户和管理员均可创建父级并调整导入类别的父级归属。已同步分类权限、树/创建/移动接口和导入预检规则，未创建数据库或业务代码。
- 确认新 POP 商品类别首次导入创建为根分类，重复导入保留员工设置的人工父级；已同步分类导入、验收和审计范围，未创建数据库或业务代码。
- 确认同一 POP 品类编码在后续批次名称变化时采用覆盖；已同步预检摘要、提交规则、人工父级保护、验收和审计要求，未创建数据库或业务代码。
- 确认 POP 分类名称按商品资料批次成功提交顺序覆盖，不比较资料日期；已从分类未决项移除日期时序，并保持售价、库存的来源日期防回退规则不变，未创建数据库或业务代码。
- 确认分类树最多 5 级且根分类为第 1 级；已同步领域词汇、分类创建/移动接口、超深错误码、前端限制、数据字典和子树移动验收，未创建数据库或业务代码。
- 确认商品只能关联叶子分类；已同步商品写接口、分类创建/移动约束、商品资料预检错误、前端限制、错误码及验收场景，未创建数据库或业务代码。
- 确认管理员允许手工修改商品所属分类、普通用户只读；已同步商品编辑接口、分类树返回字段、前端叶子选择器、ES 同步、审计和验收规则，导入覆盖优先级留作下一项确认，未创建数据库或业务代码。
- 确认管理员手工商品分类优先，后续 POP 商品资料不自动覆盖；已同步分类来源状态、预检非阻断提示、统计字段、提交规则和验收场景，未创建数据库或业务代码。
- 确认提供管理员“恢复使用 POP 分类”能力；已同步双分类引用模型、恢复接口范围、按钮状态、ES 同步、审计和验收规则，未创建数据库或业务代码。
- 确认分类停用不级联修改商品；已同步 API-057 行为、商品分类状态展示、选择限制、搜索可见性和验收场景，POP 导入遇停用分类留作下一项确认，未创建数据库或业务代码。
- 本轮首次批量补丁因数据字典原文顺序与预期不一致而整体未应用；按实际文本拆分后完成更新，没有产生部分写入。
- 确认普通员工可创建分类和调整父级，但重命名、启用及停用仅管理员可操作；已同步 API-047/API-057、前端按钮权限、403 验收和数据字典，未创建数据库或业务代码。
- 确认 POP 类别编码全局唯一且仅用于导入匹配，人工父级无业务编码；已同步分类来源模型、API-044/045/047、冲突码、字段评审和数据字典，未创建数据库或业务代码。
- 收到新的分类与导入总规则，已撤销“最近 POP 分类、分类来源状态、恢复 POP 分类接口”和后续导入覆盖分类名称的旧设计；商品暂按条码首次进入系统时以 POP 类别初始化，已有商品后续导入不改分类。
- 将导入模型收敛为【商品资料】与【每日销售汇总】两种模板，移除额外 `mode` 参数及 `PRODUCT_ONLY/PRODUCT_INVENTORY_SYNC` 模式区分；两类导入均按来源日期更新库存、当前进价和门店售价。
- 已同步总体计划、接口契约、数据字典、字段评审、领域术语、库存方案和 README；仍处于 P0 文档评审阶段，未创建数据库、迁移或业务代码。
- 用户确认“第一次初始化”按每个条码首次进入本系统判定，不按全系统首个批次判定；已从总体计划和接口契约中移除该待确认项。
- 完成 M03 两项关键颗粒度对齐：当前进价改为按“门店 + 商品 + 供应商”保存，商品资料和每日销售汇总只更新目标门店组合；同步调整实体职责、管理员视图、API-037/API-038 路径、导入定位与跨门店隔离验收。
- 纠正 POP 分类规则：POP 源端不存在分类停用，导入不设计相关阻断或自动处理；同一条码首次导入确定商品初始分类，后续即使品类不同也保留系统当前分类，管理员可在系统内修改。已同步总体计划、接口契约、数据字典、字段评审、领域词汇和 README，未创建数据库、迁移或业务代码。
- 开始 M02“用户与门店”独立粒度规划；已在总计划登记 P0-M02 子阶段，并确认本轮按逐步确认门禁执行：每次只确认一个主题，全部确认后才把 `docs/api/02-user-store.md` 标为 `APPROVED`，不创建数据库、迁移或业务代码。
- 已创建 `docs/api/02-user-store.md` 完整评审草案并加入 README 导航，覆盖 API-013～025、8 步确认清单、用户/门店生命周期、seam、权限、DTO、并发、审计、页面与验收；当前仅等待第 1 步“角色模型与账号门店绑定基线”确认，后续章节均未确认。
- 用户确认 M02 第 1 步通过；已记录固定角色、管理员无门店、普通用户唯一门店、一个门店多普通用户和服务端绑定权威规则，并同步 `CONTEXT.md` 领域术语。当前进入第 2 步“用户生命周期、角色切换与门店改绑”。
- 根据用户对 M02 第 2 步的修改意见，已从评审稿移除用户启用/停用状态，把 API-017 草案改为管理员删除普通用户账户，并同步调整门店停用前置条件、权限、DTO、错误码、页面和验收；第 2 步仍为 `awaiting_confirmation`。
- 根据补充意见，已扩展第 2 步：管理员可创建两类账户，API-017 同时支持删除普通用户和注销管理员；允许管理员注销自己或其他管理员，但保留“系统至少一个管理员”约束。第 2 步仍等待明确确认。
- 用户确认 M02 第 2 步通过；已将无用户状态、两类账户创建、普通账户删除、管理员注销、最后管理员保护、不可恢复删除、历史身份保留及会话失效规则同步至 M02、总接口稿、实施计划、数据字典、领域术语、README 和 M01 依赖表述。当前进入第 3 步。
- 已细化 M02 第 3 步推荐方案：用户名小写规范化、4～32 位格式与永久保留；显示名独立；仅管理员设置或重置任一账户密码；重置使目标全部会话失效；M02 复用 M01 密码策略；首版无自助改密、找回密码、强制改密、有效期或密码历史。当前等待确认。
- 根据用户补充，已在第 3 步增加本人资料编辑：新增 API-019，管理员与普通用户均可修改本人姓名、性别、出生日期、入职日期，年龄由出生日期自动计算；本人接口不能修改用户名、角色、门店或密码。年龄存储方案仍等待用户确认。
- 2026-09-13：用户选择直接保存手工年龄；已从 M02 草案移除 `birthDate` 和自动年龄计算，统一改为可编辑 `age`，并同步领域术语、页面提示和验收。第 3 步整体仍等待确认。
- 2026-09-13：用户确认 M02 第 3 步通过；已将用户名、个人资料、手工年龄、管理员密码重置与会话失效规则同步至 M02、总接口稿、M01 会话视图、实施计划、数据字典、领域术语和 README。当前进入第 4 步门店生命周期评审。
- 2026-09-13：根据用户补充将 M02 第 4 步改为支持门店物理删除；新增“仅已停用且无账号/业务引用的空门店可删、禁止级联清理、并发时事务内复核”草案，并预留 API-026、错误码、页面提示和验收场景。第 4 步仍为 `awaiting_confirmation`，未同步未确认规则到总接口稿。
- 2026-09-13：用户确认 M02 第 4 步通过；已将门店启停、安全物理删除、无级联清理和并发复核规则同步至 M02、总接口稿、实施计划、数据字典、领域术语与 README，并将 API 范围扩展至 API-026。当前进入第 5 步门店字段与唯一性评审。
- 2026-09-13：用户确认 M02 第 5 步通过；已将门店编码格式、现存唯一性、创建后不可修改、空门店删除后可复用、名称可重复和联系方式规则同步至 M02、总接口稿、实施计划、数据字典、领域术语与 README。当前进入第 6 步 API、DTO、分页和并发评审。
- 2026-09-13：已完善 M02 第 6 步待确认草案：补齐列表筛选与分页封装、详情 DTO、HTTP 成功状态、新门店默认启用、密码重置版本校验，并统一 DELETE 使用 `If-Match` 及安全重试语义；尚未把这些未确认细节同步到总接口稿。
- 2026-09-13：用户确认 M02 第 6 步通过；已将 API-013～026、DTO、分页、HTTP 成功状态、新门店默认启用、乐观锁与删除重试规则同步至总接口稿、实施计划、数据字典和 README。当前进入第 7 步 seam、权限、审计与安全评审。
- 2026-09-13：已完善 M02 第 7 步待确认草案：修正无用户状态后的 seam，增加 M01 会话撤销端口、跨模块门店引用探针和 M10 审计端口，明确失败关闭、本人资料对象来源、审计范围、字段白名单及 CSRF/密码保护；未同步未确认细节到总接口稿。
- 2026-09-13：用户确认 M02 第 7 步通过；已将模块 seam、后端权限、跨模块引用检查失败关闭、审计与敏感信息保护同步至总接口稿、实施计划、数据字典和 README。当前进入最终第 8 步页面、任务、测试与完成门禁评审。
- 2026-09-13：已复核并扩充 M02 最终第 8 步草案，覆盖三类页面、前后端实施任务、分层测试、字段/权限/并发/会话/门店删除验收和最终定稿门禁；第 8 步仍为 `awaiting_confirmation`，未提前将 M02 标为完成。
- 2026-09-13：用户确认 M02 第 8 步通过；已将 8/8 步标记为 `confirmed`、M02 文档状态改为 `APPROVED`、P0-M02 状态改为 `complete`，并同步页面、测试、README 与实施计划状态。下一步执行最终静态一致性校验；本轮未创建数据库、迁移或业务代码。
- 2026-09-13：M02 最终静态一致性校验完成：8 个步骤全部确认，API-013～026 在模块稿与总接口稿中完整一致且无全局编号重复，项目 Markdown 本地链接和代码围栏有效，`git diff --check` 通过。M02 正式定稿，未创建数据库、迁移或业务代码。
- 按用户确认的新全局规则同步 M04、M05、总接口契约、实施计划、数据字典、领域术语、ADR、字段评审和 README：当前进价、售价、库存改为 POP 导入专属写入；跨批次按成功提交顺序覆盖；同批次按原 Excel 行号取最后一个非空值并提示；来源日期只追溯。M04 第 8 步仍等待整体确认，未进入第 9 步，也未创建数据库、迁移或业务代码。
- 用户于 2026-09-13 确认 M04 第 8 步通过；已更新逐步确认清单并开放第 9 步评审。M04 仍为 `in_progress`，第 10 步保持未开放，未创建数据库、迁移或业务代码。
- 第 9 步评审前将 API-075 的并发版本传递固定为 `If-Match` 请求头，不再保留 DELETE 请求体或查询参数二选一；API-074 继续使用请求体中的 `expectedVersion + previewToken`。
- 用户于 2026-09-13 确认 M04 第 9 步通过；已更新确认清单并开放最终第 10 步。同步修正页面旧文案为“覆盖/未变化/重复值提示”，只有零销量行使用“忽略”；M04 仍为 `in_progress`，等待第 10 步确认后再执行定稿校验。
- 修正 M04 完成定义中的跨模块前置条件：M04 可在自身调用边界确认后定稿，不被尚未生成的 M03/M06 独立文档无限阻塞；后续模块评审若改变该边界，必须重新打开受影响的 M04 步骤。
- 用户于 2026-09-13 确认 M04 第 10 步通过；已将 10/10 步标记为 `confirmed`、M04 文档状态改为 `APPROVED`、P0-M04 状态改为 `complete`，并同步 README 与实施计划状态。下一步执行最终静态一致性校验；本轮未创建数据库、迁移或业务代码。
- M04 最终静态校验通过：确认清单 10/10、API-060～075 在独立稿和总接口稿中完整一致、15 个项目自有 Markdown 文件代码围栏与本地链接有效、旧日期跳过/人工改价等有效规则无残留，`git diff --check` 通过。首次链接扫描误纳入 `node_modules` 的第三方 README，已记录并通过限定项目文件范围解决。
- 用户于 2026-09-13 明确确认 M01 第 8 步；M01 的 8 个步骤现已全部确认。已将 `docs/api/01-system-auth.md` 标记为 `APPROVED`，并把 Redis 服务端会话、Cookie/CSRF、健康检查、权限 code、管理员重置密码和敏感信息规则同步至总接口稿、实施计划、数据字典、领域术语与 README。
- M01 定稿静态校验完成：确认表 8/8 均为 `confirmed`，API-001、002、009～012 编号和路径完整，总接口稿无正向 Bearer/JWT/access-token 认证设计，Markdown 代码围栏与本地链接有效，`git diff --check` 通过。本次只修改规划文档，未创建数据库、迁移、Redis 键或业务代码。
- 2026-09-13：完成 M03“门店商品供应商关系生命周期”对齐：移除 API-038 及供应商人工写入/启停设计；POP 导入直接 upsert 文件中出现的门店 + 商品 + 供应商记录，新名称无需预建，未出现的旧记录保留；普通用户查看本店全部供应商名称。已同步总计划、接口契约、M04 导入规则、数据字典、字段评审、领域术语与 README，未创建数据库、迁移或业务代码。
- 2026-09-13：收到“同一 POP 表格中同条形码、不同供应商直接覆盖更新”的补充；由于“仅本批最后行胜出”与“替换并移除系统旧供应商”会产生不同的数据基数，已登记为 M03 精确边界，等待一句确认后再同步核心契约，避免与此前旧记录保留规则冲突。
- 2026-09-13：用户确认采用“B 替换 A，系统只保留 B”。已将 M03/M04 从门店商品累计多供应商模型改为唯一当前供应商模型：同条码非空新供应商替换旧关联，同文件最后一行决定供应商及配套进价，商品未出现或供应商为空时保留当前值；同步更新 DTO、API-037、数据约束、页面与验收口径。
