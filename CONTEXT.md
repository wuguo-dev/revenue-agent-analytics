# 销售分析领域

本文件统一项目中的业务语言，避免把销售事实、POP 库存观察值和系统推算值混为一谈。

## Language

**管理员账号**：
固定使用 `ADMIN` 角色且不绑定门店的系统账号，其管理范围覆盖全部门店；管理员可以创建管理员和普通用户账户，也可以在系统仍保留其他管理员时注销自己或其他管理员。
_Avoid_: 默认门店管理员、门店管理员

**普通用户账号**：
固定使用 `USER` 角色且必须唯一绑定一个启用门店的系统账号；一个门店可以绑定多个普通用户。
_Avoid_: 多门店账号、未归属账号

**账号门店绑定**：
普通用户账号与唯一门店之间的数据范围关系，由服务端账号记录作为权威来源，不能由客户端提交的门店参数替代。
_Avoid_: 客户端选店、默认门店、临时门店

**认证会话**：
登录成功后由 M01 在 Redis 中建立的服务端会话，浏览器只用不透明 `HttpOnly` Cookie 携带会话标识；空闲 30 分钟滑动续期、绝对 12 小时，每个账号最多 3 个并发会话。
_Avoid_: 前端 JWT、localStorage token、无限期登录

**当前认证主体**：
M01 根据有效会话和 M02 权威账号数据构造的服务端身份，包含用户、固定角色、门店范围和权限 code；业务模块不得相信客户端自行提交的角色或门店。
_Avoid_: 客户端身份、前端权限即授权、请求参数门店即数据范围

**账户删除或注销**：
管理员对普通用户执行删除、对管理员执行注销后，目标账户立即失去登录能力且不可恢复；系统保留最小历史身份并永久保留原用户名，且始终至少保留一个管理员账户。
_Avoid_: 用户停用、可恢复删除、用户名回收

**门店停用**：
门店退出新业务操作但继续保留身份和全部历史引用的状态；停用前必须先改绑或删除尚未删除的普通用户，停用不自动清理账号或业务数据。
_Avoid_: 删除门店、清空门店历史

**门店标识**：
业务数据只引用不可变的门店内部 ID；门店编码是现存门店中唯一、创建后不可修改的展示标识，名称允许重复。空门店物理删除后编码可以复用，但复用编码的新门店具有新的内部 ID。
_Avoid_: 门店名称主键、按名称关联门店、复用原门店 ID

**门店物理删除**：
仅对已停用、无未删除用户绑定且无任何主数据或业务数据引用的空门店执行的不可恢复移除；存在任一引用时只能停用，禁止级联删除、迁移或改写历史数据。
_Avoid_: 带数据删店、级联清店、软删除门店

**员工年龄**：
用户个人资料中由本人或管理员手工维护的当前周岁，不保存出生日期，也不会随时间自动更新。
_Avoid_: 出生日期推算年龄、自动递增年龄

**用户个人资料**：
账号对应的姓名、性别、手工年龄和入职日期；本人和管理员均可维护，但它不包含用户名、角色、账号门店绑定或密码。
_Avoid_: 登录凭据、权限资料、自动年龄

**商品条形码**：
商品当前使用的唯一业务匹配标识，由 1 至 64 位数字或英文字母组成并保留前导零；只能由 POP 导入创建，进入系统后任何角色均不能修改。
_Avoid_: 数据库主键、可编辑条码、历史匹配别名

**POP 权威库存**：
POP 系统对某门店、某商品给出的实际库存，是本系统唯一认可的库存来源。
_Avoid_: 理论库存、系统计算库存

**最新已知库存**：
本系统最近一次从 POP 商品资料表或每日销售表中观察到的门店商品库存。它不一定代表查询时刻的实时库存。
_Avoid_: 实时库存、绝对当前库存

**POP 商品资料导出集**：
POP 可为单一门店导出全部商品及其最新库存，范围包含零库存和已停用商品。
_Avoid_: 系统导入批次

**尚未同步到本系统的商品**：
商品已经存在于 POP 的完整商品资料中，但尚未通过商品资料导入批次进入本系统。
_Avoid_: 未入库、新进货、POP 缺失商品

**商品资料导入批次**：
一次实际提交到本系统的商品资料集合，可以是完整导出集的一部分；新条码新增到商品主档，已有条码更新，批次中未出现的商品不代表缺失、停用或库存为零。
_Avoid_: 强制全量快照、进货、库存累加

**商品主档**：
系统持续维护的商品基础资料集合，可以通过多个商品资料批次逐步建立和更新，并非只能初始化一次。
_Avoid_: 初始商品资料、一次性商品库

**商品物理删除**：
管理员对商品及其全部当前关系和历史业务数据执行的不可恢复移除；删除后不保留商品身份、逻辑删除标记、审计或该商品的可追溯历史，同一条码以后再次导入时视为全新商品。
_Avoid_: 商品停用、逻辑删除、商品墓碑、历史保留

**商品分类**：
全系统共享且最多五级的商品分组，根分类为第一级；只有叶子分类可以直接归属商品。停用分类仍标识已有商品，但不再接受新的商品归属。
_Avoid_: 门店分类、仅一级品类、POP 固定层级

**POP 商品类别**：
商品通过商品资料首次进入本系统时随商品带入的初始分类，以 POP 品类编码作为全系统唯一匹配标识；POP 源端不会停用类别，商品后续即使以同一条码携带不同类别再次导入，也保留系统中的当前商品分类。
_Avoid_: 自动父级、门店类别、停用 POP 类别

**手工商品分类**：
管理员在系统页面为商品指定的当前叶子分类；保存后作为唯一有效分类，后续商品资料和每日销售导入都不会覆盖。
_Avoid_: 临时分类、导入后自动还原、最近 POP 分类

**人工父级分类**：
普通员工或管理员为整理全局分类树而创建的父级目录，只需名称，不使用 POP 品类编码；后续商品资料导入不会撤销已经完成的人工归类。
_Avoid_: POP 推断父级、导入临时目录、人工品类编码

**门店售价**：
某一门店为某一商品维护的当前销售标价；同一商品在不同门店可以有不同售价。
_Avoid_: 商品全局售价、统一售价

**门店供应商当前进价**：
某一供应商当前向某一门店提供某一商品的最新已知进价，由该门店最后成功提交的商品资料或每日销售汇总刷新，只用于管理员查看当前供货信息，不代表历史销售成本；任何角色均不能手工修改。
_Avoid_: 商品统一成本、跨门店统一进价、历史销售成本、进价历史

**门店商品供应来源**：
某一门店中某个商品当前对应的唯一供应商名称文本，仅用于让员工了解货品来源；不建设独立供应商主档或供应商 ID，也不提供人工维护。名称去除首尾半角/全角空白后保存，清洗后为空或商品未出现时保留当前值，内部字符不做归一或合并。
_Avoid_: 供应商实体、供应商 ID、累计供应商列表、可人工维护的供应关系、供应商状态、销售分析维度

**进价来源日期**：
门店供应商当前进价所依据的 POP 业务日期，仅用于追溯；商品资料取资料日期，每日销售汇总取营业日期，不参与覆盖优先级判断。
_Avoid_: 导入时间、历史销售成本日期

**售价来源日期**：
门店售价所依据的 POP 业务日期，仅用于追溯；商品资料导入取资料日期，每日销售导入取营业日期，不参与覆盖优先级判断。门店售价不提供人工修改。
_Avoid_: 导入时间、售价修改时间

**资料日期**：
一份 POP 商品资料所代表的数据日期，由管理员在导入前选择，作为其中进价、售价和库存的追溯字段，不决定当前值覆盖顺序。
_Avoid_: 文件上传日期、提交日期

**目标门店**：
一次导入批次唯一归属的门店；POP 字段中的“当前机构”或“选中机构”均指该门店。
_Avoid_: 当前机构、任意门店

**部分库存快照**：
一次每日销售导入中出现的商品及其 POP 库存集合；未出现的商品不属于该快照，也不代表库存为零。
_Avoid_: 全量库存、缺失即零

**销售事实**：
某门店、某营业日期、某商品的销售数量、销售收入、成本和毛利数据，与库存变化的计算相互独立。
_Avoid_: 库存流水

**零销量忽略行**：
每日销售导入中“本期销售数量”等于 0 的源数据行；该行整行舍弃，不参与销售事实、门店售价或门店库存同步。
_Avoid_: 零销售事实、库存刷新行

**销售毛利率快照**：
POP 每日销售汇总对某门店、营业日期和商品给出的毛利率事实，用于历史销售分析，不等同于商品资料中的参考毛利率。
_Avoid_: 商品当前毛利率、按当前进价重算值

**销售查询窗口**：
一次销售查询所选择的连续日期区间，最多包含 31 个自然日；它不表示销售历史只保留 31 天。
_Avoid_: 销售保留期限、最近一个月数据

**同比区间**：
主查询区间向前推一个自然年的对应日期区间，由系统历史销售事实计算。
_Avoid_: POP 同期列、用户手填对比区间

**环比区间**：
紧邻主查询区间之前且包含相同自然日数量的连续区间，由系统历史销售事实计算。
_Avoid_: POP 同期列、任意上一段时间

**营业日期**：
销售事实所属的业务日期，由导入人员在导入前选择。
_Avoid_: 导入时间、库存更新时间

**库存观察时间**：
系统接收并确认某个 POP 库存数值的时间，用于判断库存新鲜度。
_Avoid_: 营业日期

**库存新鲜度**：
以最近成功提交并应用当前库存投影的观察时间，按 Asia/Shanghai 自然日计算的状态；默认超过 3 个自然日未刷新为陈旧。任何日期的后续成功 POP 批次都会刷新该时间。陈旧库存每天凌晨 04:00 扫描。
_Avoid_: 仅按来源日期判断新鲜度

**库存告警确认**：
管理员对负库存或陈旧库存异常表示已经知悉，不等于异常已经解决；只有库存恢复非负或数据重新变新鲜时由系统自动恢复告警。
_Avoid_: 人工修改库存、异常仍存在时强制关闭

**总库最新已知库存**：
所有门店、所有商品各自最新已知库存的求和；其中不同商品的库存观察时间可能不同。
_Avoid_: 实时总库存

**业务审计日志**：
系统对关键业务操作的只读追踪记录，用于说明谁在何时对什么对象执行了什么操作以及结果如何。
_Avoid_: 销售流水、程序运行日志、错误堆栈
