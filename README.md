# 多 Agent 协作销售分析系统

面向内部零售运营的销售分析平台，计划通过 POP 商品资料与每日商品销售汇总，提供商品目录、门店库存、销售分析、冷热商品分析，以及管理员专用的多 Agent 协作分析能力。

> 当前状态：**P0 计划与接口评审阶段**。业务数据库字段、建表 SQL 和模块接口尚未最终确认；仓库当前不是可运行成品，请勿据此部署生产环境。

## 当前工作原则

项目采用“计划确认 → 分模块接口评审 → 数据模型评审 → 后端接口与测试 → 对应前端页面 → 浏览器联调验收”的垂直切片方式开发。

- 在总体计划和字段模型确认前，不创建正式业务表或 Flyway 迁移。
- 总体计划定稿后，将 M01–M10 分别生成独立接口文档。
- 所有模块规划逐步评审；每一步必须获得业务方明确确认并留下记录，才能继续下一步。
- 每个后端功能必须同步实现对应前端页面，并通过真实接口进行浏览器测试。
- 当前仓库中的前端文件仅为早期界面草案；后端工程已从未评审草案中移除。

## 已确认的核心业务规则

- 系统固定管理员和普通用户两类角色；管理员不绑定门店并可创建两类账户，普通用户唯一绑定一个启用门店，一个门店可绑定多个普通用户。
- 账号由管理员统一创建和分发，员工没有修改密码权限；只有管理员可以设置或重置密码。
- 管理员和普通用户均可在“我的资料”编辑姓名、性别、手工年龄和入职日期；年龄不会自动增长，不采集出生日期，个人资料接口不能修改用户名、角色、门店或密码。
- 登录使用 Redis 服务端会话和 `HttpOnly` Cookie，不把认证凭据写入浏览器 Web Storage；会话空闲 30 分钟、绝对 12 小时，每账号最多 3 个并发会话。
- 用户不设置启用/停用状态；管理员可删除普通用户，也可在系统仍保留其他管理员时注销自己或其他管理员。删除或注销立即失去登录能力、全部会话失效且不可恢复，历史业务与审计仍保留原操作人身份。
- 门店采用启用/停用状态；有关联普通用户时不能停用，停用后历史数据继续保留。仅已停用且无账号或业务引用的空门店可以物理删除，禁止级联清理历史数据。
- 门店编码规范化后在现存门店中唯一且创建后不可修改，空门店物理删除后可复用编码但使用新的内部 ID；门店名称允许重复。
- 用户和门店写接口采用乐观锁：PUT 提交请求体版本，DELETE 使用 `If-Match`；版本冲突不覆盖最新数据，删除网络重试不重复审计。
- 用户与门店管理权限由后端强制执行；本人资料只能作用于当前登录者。门店引用检查不完整时禁止物理删除，密码、会话和完整请求体不得进入日志或审计。
- 普通用户可查看所属门店的条形码、供应商、商品名称、规格、单位、分类、售价、状态、最新已知库存及同步时间。
- 商品单位直接保存为文本，不建立单位字典；管理员可修改，普通用户只读。
- 商品分类全系统共享并支持最多 5 级父子层级，根分类计为第 1 级；商品资料带入商品类别，普通用户和管理员都可创建父级分类并调整类别归属。
- 商品只能关联没有子分类的叶子分类；已有商品的分类不能再增加子分类，人工父级主要用于组织分类目录。
- 管理员可以在商品编辑页将商品调整至其他启用的叶子分类，普通员工只能查看商品分类。
- 商品通过商品资料首次进入本系统时，使用该行 POP 商品类别初始化其分类；同一条码后续即使导入了不同品类，也继续使用系统中的当前分类，不被导入覆盖。
- 管理员可在商品页面手工调整已有商品分类；系统不保存“最近一次 POP 分类”，也不提供恢复 POP 分类操作。
- 系统内分类停用不级联停用或移动商品；已有商品继续显示原分类及停用标记，但该分类不能再用于新的商品归属。POP 源端不会停用分类，因此导入不处理“新商品命中已停用 POP 分类”的分支。
- 首次导入的新 POP 类别创建为根分类；人工归入父级后，后续商品资料导入不得重置该父级关系。
- 已存在的 POP 分类名称和父级关系不再被后续导入覆盖，分类结构与名称统一在系统页面维护。
- 普通员工可创建分类和调整父级，但分类重命名、启用或停用仅允许管理员操作。
- POP 类别以品类编码全局唯一匹配；人工父级只填写名称，不要求员工编写分类编码。
- 同一商品的当前售价按门店分别保存；普通用户只查看所属门店售价，管理员可查看各门店售价，但不提供手工改价。
- 当前进价、门店售价和库存统一由 POP 商品资料或每日销售汇总导入更新，不提供手工修改当前值；资料日期和营业日期只用于追溯，不决定覆盖优先级。
- 每日销售表中的“当前机构售价”对应目标门店；最后成功提交的 POP 导入批次直接覆盖本店当前售价，即使该批次营业日期较旧。
- 普通用户可为所属门店导入每日销售汇总并查看本店导入批次，但不能查看销售金额、成本、利润、毛利率、其他门店库存或 AI 功能。
- 每个门店商品只保留一个当前供应商，用于展示商品来源，不作为销售分析维度。同一条形码出现非空新供应商时，新供应商覆盖旧供应商；同一文件重复条码按 Excel 原始行号最后一行决定供应商。
- 当前进价随当前“门店 + 商品 + 供应商”组合保存，只保留最后成功提交 POP 批次的进价及来源日期；商品资料和每日销售汇总只更新目标门店，不能改动其他门店进价。系统不建设历次进价历史，历史销售成本仍以每日销售事实快照为准，普通用户不能查看进价。
- 供应商及供应关系不设置启用/停用状态，也不提供人工维护入口；POP 导入中的非空新供应商直接替换本店商品的旧供应商，商品未出现或供应商为空时保留当前供应商。普通用户查看所属门店商品的当前供应商名称。
- 商品资料中的毛利率不入库；历史毛利分析仅使用每日销售汇总中的销售毛利率快照。
- 每日销售表中的三列同期值不入库；同比和环比由系统历史销售事实计算，历史不足时显示暂无可比数据。
- 商品当前条码全局唯一，按 1–64 位数字或英文字母字符串保存并保留前导零；管理员可修改，普通用户不可修改。
- POP 中商品和库存数据完整，POP 库存是本系统唯一权威库存来源。
- 商品资料可以分多个批次逐步同步到本系统；每批只新增或更新出现的条码，批次外商品保持不变。
- 商品资料导入前由管理员必选资料日期，用于追溯该批次进价、门店售价和库存对应的 POP 数据日期；较旧日期的批次如果后提交，仍覆盖当前值。
- 已有商品采用非空覆盖：空单元格保留旧值，有效非空值才覆盖；空值不表示清空或写零。
- 系统只定义【商品资料】和【每日销售汇总】两种导入模板；两种模板中的有效库存、进价和售价均按最后成功提交批次刷新对应当前值，不比较来源日期。
- 商品资料和每日销售文件中的有效库存值直接覆盖对应门店商品库存，不使用销量推算库存。
- 库存新鲜度默认阈值为 3 个自然日；系统每天凌晨 04:00（Asia/Shanghai）扫描陈旧库存，首版只生成站内陈旧/负库存告警。
- 每日销售文件中“本期销售数量”为 0 的行整行舍弃，不创建销售事实，也不更新当前进价、门店售价或库存；负销量仍按退货事实处理。
- 每日销售导入前必须选择营业日期；销售事实长期保留，单次主查询区间最多连续 31 个自然日，同比和环比由后端自动读取历史对比区间。
- 多 Agent 分析仅管理员可用，Agent 只能调用预定义的聚合查询工具，不能执行自由 SQL。
- 业务审计日志仅管理员可查，用于追踪关键操作；它不保存密码、密码哈希、Cookie、会话标识、CSRF token、API Key 或完整 Excel 内容，供应商及报价变化不进入审计。

## 计划技术栈

- 后端：Java 17、Spring Boot 3.5.8、Spring AI Alibaba 1.1.2.2、Spring AI 1.1.2、MyBatis-Plus、Spring Security、Flyway、Apache POI。
- 前端：Vue 3、TypeScript、Vite、Pinia、Vue Router、Element Plus、ECharts、Axios。
- 数据与基础设施：MySQL、Redis、Elasticsearch；生产环境计划切换阿里云 RDS MySQL、Redis 和 Elasticsearch 服务。
- AI 模型：阿里云百炼 Qwen，通过 DashScope 接入。

## 仓库目录

```text
frontend/       早期前端界面草案，尚未进入正式功能开发
deploy/         MySQL、Redis 的本地编排与环境变量示例
docs/           计划、接口清单、字段评审、库存方案和架构决策
CONTEXT.md      项目领域术语
task_plan.md    阶段状态与实施门禁
findings.md     已确认业务发现
progress.md     规划过程记录
```

后端工程将在计划、模块接口和数据模型完成评审后，按确认结果创建。

## 规划文档

- [总体实施计划](docs/implementation-plan.md)
- [接口清单评审稿](docs/api-contract.md)
- [M01 系统与认证接口定稿](docs/api/01-system-auth.md)
- [M02 用户与门店接口定稿](docs/api/02-user-store.md)
- [M03 商品与基础资料逐步确认稿](docs/api/03-catalog-search.md)
- [M04 导入中心接口与实施粒度规划](docs/api/04-imports.md)
- [POP 源表字段评审](docs/source-field-review.md)
- [POP 权威库存方案](docs/inventory-source-assessment.md)
- [数据字典初稿](docs/data-dictionary.md)
- [领域术语](CONTEXT.md)
- [ADR：POP 是库存的唯一权威来源](docs/adr/0001-pop-authoritative-inventory.md)

`docs/api-contract.md` 是跨模块接口总清单；M01、M02、M04、M05 已确认定稿，其余模块按总体计划逐个拆分。

## 后续节点

1. 完成总体实施计划评审并标记为 `APPROVED`。
2. 生成并评审 M01–M10 独立接口文档。
3. 确认 POP 字段取舍、业务口径、ER 图、索引、外键和建表 SQL。
4. 经用户确认后创建数据库迁移和正式后端工程。
5. 按模块同步交付后端接口、前端页面和浏览器联调测试。

## 待评审的初始愿景

远程仓库最初提出通过多 Agent 生成每日日报、周报，并在商品库存达到警戒条件时发送提醒。该方向予以保留，但报表生成频率、库存警戒规则、通知方式和首版范围仍需在总体计划中确认。

## 安全说明

- 不要提交 `.env`、访问令牌、数据库或 Redis 凭据、会话标识、CSRF token 或 DashScope API Key。
- `deploy/.env.example` 只提供变量名和示例占位值，实际密钥应通过本地环境或部署平台注入。
