# 多 Agent 协作销售分析系统实施计划（评审稿）

> 文档状态：DRAFT。用户确认并改为 APPROVED 前，禁止新增业务代码、数据库迁移和功能页面。

## 1. 项目目标

建设一套内部销售分析系统：管理员导入 POP 商品资料和每日销售汇总，管理门店商品与库存，查看销售、毛利及冷热商品分析，并通过固定多 Agent 工作流获得分析报告；普通用户可以查看所属门店的非敏感商品资料及最新已知库存，并可为所属门店导入每日销售汇总，但不能查询销售、成本、利润等经营数据。

## 2. 首版范围

### 包含

- 管理员、普通用户两类角色；普通用户绑定一个门店。
- 门店、仓库与货位信息；仓库只表示位置，不保存库存。
- 全局商品档案、门店独立库存、门店库存汇总。
- `.xls/.xlsx` 商品资料和每日销售汇总导入。
- 管理员和普通用户均可导入每日销售汇总；普通用户的门店由登录账号确定，导入前必须选择营业日期。
- POP 中的商品及库存数据完整；商品资料导入支持从 POP 分批同步到新系统，并可在后续周期性刷新 POP 库存，不提供进货累加库存模式。
- 每日销售汇总按 POP 库存直接覆盖文件中出现商品的门店库存，不以销量计算库存；支持负库存和数据陈旧告警。
- 销售、毛利、门店对比、商品排行、冷热商品分析。
- 管理员专用多 Agent 对话、SSE 进度、图表和历史报告。

### 不包含

- 用户注册、多租户、移动端、小程序。
- 仓库级库存、采购订单、仓库调拨、会员和收银。
- 微服务、消息队列、MinIO。Elasticsearch 属于首版核心组件，仅用于商品检索。
- 动态自主 Agent、自由 SQL、普通用户 AI。

## 3. 开发规则与门禁

每个节点固定按“确认输入 → 数据库评审 → 后端接口与测试 → 前端页面 → 真实接口联调 → 用户验收”执行。后端接口没有对应页面，或页面仍使用模拟数据，均不算完成。

| 门禁 | 确认内容 | 通过条件 |
|---|---|---|
| G0-A 计划 | 范围、节点、模块边界、验收方式 | 本计划标记 APPROVED |
| G0-B 接口 | 每个业务模块的接口、权限、DTO、错误和页面映射 | `docs/api/` 下模块接口文档全部通过评审 |
| G1 数据 | 两类脱敏样表、字段、类型、唯一键、关系 | ER 图、字段字典、映射表通过评审 |
| G2 权限 | 角色能力、门店数据范围、公开字段 | 权限矩阵通过评审 |
| G3 导入 | 预检、幂等、错误、覆盖和回滚规则 | 导入状态机与样例结果通过评审 |
| G4 指标 | 销量、金额、毛利、增长、冷热口径 | 指标字典和样例计算通过评审 |
| G5 上线 | 环境变量、备份、日志、阿里云配置 | 部署与恢复演练通过 |

## 4. 架构基线

- 后端：Java 17、Spring Boot 3.5.8、Spring AI 1.1.2、Spring AI Alibaba 1.1.2.2、MyBatis-Plus 3.5.17、Spring Security、Flyway、Apache POI。
- 前端：Vue 3、TypeScript、Vite、Pinia、Vue Router、Element Plus、ECharts、Axios。
- 开发基础设施：本地 MySQL、Redis、Elasticsearch；生产使用阿里云 RDS MySQL、Redis 和 Elasticsearch 服务。
- AI：阿里云百炼 Qwen；API Key 缺失时仅禁用 AI 模块，不影响其他功能。
- 模块：`security`、`organization`、`catalog`、`search`、`imports`、`inventory`、`sales`、`analytics`、`agent`、`audit`。
- MySQL 是商品权威数据源；Elasticsearch 只保存商品搜索索引。无消息队列条件下采用数据库同步任务表与后台重试，支持全量重建。
- Agent 只能调用应用内参数化分析服务，不接触数据库连接和任意 SQL 工具。

## 5. 数据模型确认节点

在提供真实脱敏样表前，只确认实体职责，不确定最终列和 SQL。

| 实体 | 职责 | 已确认规则 |
|---|---|---|
| User / Role | 登录、角色、门店绑定 | 用户名唯一；普通用户绑定一个门店 |
| Store | 销售与库存归属 | 门店编码规则待确认 |
| Product | 全局商品档案 | 条形码业务唯一；管理员可受控修改，内部商品 ID 和历史关系不变 |
| StoreProduct | 门店商品状态、当前售价与最新已知库存 | 门店 + 商品唯一；同一商品允许各店售价不同并保存售价来源日期；库存只由 POP 覆盖并保留来源日期和最后同步时间 |
| Supplier / Category | 商品基础资料 | 唯一规则待确认 |
| ProductSupplier | 商品供货来源和供应商当前进价 | 商品 + 供应商唯一；只保留最新进价及其来源日期，不建设历次进价历史，不作为销售分析维度 |
| Warehouse / Location | 存放位置 | 不保存库存数量 |
| ProductLocation | 商品多货位关联 | 商品 + 货位唯一 |
| ImportTemplate / ImportBatch | 映射、预检、幂等、审计 | 销售批次按门店 + 营业日期 + 类型 + 文件指纹防重复 |
| InventorySnapshot / InventorySyncRecord | POP 库存批次与覆盖记录 | 商品资料和销售表均按批次内条码局部覆盖；记录覆盖前后值、来源批次和时间 |
| DailySales | POP 每日销售快照 | 门店 + 日期 + 商品唯一 |
| ScoreConfig / ScoreResult | 冷热规则与快照 | 权重合计 100% |
| Conversation / AgentRun | 对话、步骤、报告、图表 | 仅管理员访问 |

G1 必须产出：完整 ER 图、逐字段数据字典、两类 Excel 映射表、索引与外键清单、建表 SQL 评审稿。用户确认后才创建 Flyway `V1`。

## 6. 业务模块与接口文档交付约定

项目计划先完成评审并标记 `APPROVED`。计划定稿后、数据库设计前，再依据下表拆分业务模块，并为每个模块单独生成接口文档；当前 `docs/api-contract.md` 仅作为接口清单评审稿，不作为最终模块接口文档。

| 编号 | 业务模块 | 主要职责 | 定稿后接口文档 |
|---|---|---|---|
| M01 | 系统与认证 | 健康检查、登录、退出、当前用户、令牌失效 | `docs/api/01-system-auth.md` |
| M02 | 用户与门店 | 用户、角色、门店、账号门店绑定 | `docs/api/02-user-store.md` |
| M03 | 商品与基础资料 | 商品、供应商、分类、仓库货位、ES 商品搜索 | `docs/api/03-catalog-search.md` |
| M04 | 导入中心 | 模板、上传、映射、预检、批次、提交与错误下载 | `docs/api/04-imports.md` |
| M05 | POP 库存 | 最新已知库存、商品/销售批次覆盖、同步记录、新鲜度、告警 | `docs/api/05-inventory.md` |
| M06 | 每日销售 | 每日销售事实、退货事实、修正版 | `docs/api/06-sales.md` |
| M07 | 经营分析 | 概览、趋势、门店对比、商品排行、毛利和异常 | `docs/api/07-analytics.md` |
| M08 | 冷热商品 | 评分配置、评分运行、结果和解释 | `docs/api/08-scoring.md` |
| M09 | 多 Agent | 会话、运行、SSE、图表和报告 | `docs/api/09-agent.md` |
| M10 | 审计与运维 | 审计日志、配置状态、运行状态 | `docs/api/10-audit-operations.md` |

`docs/api/00-conventions.md` 统一定义认证、分页、金额日期精度、错误码、幂等和 SSE，不替代各模块文档。每份模块文档必须独立包含：模块边界、权限矩阵、接口清单、完整请求/响应字段及示例、校验规则、错误码、幂等与事务、前端页面/操作映射、接口测试和页面验收场景。

## 7. 开发节点与接口范围

### P0：计划与接口确认（当前）

- 先完成本文档评审并标记 `APPROVED`，通过 G0-A。
- 计划定稿后，按 M01–M10 生成独立接口文档，逐模块确认接口、权限、请求响应、错误场景和前端页面映射，通过 G0-B。
- 当前 `api-contract.md` 保留为拆分前的总接口清单；模块文档完成后改为索引或归档，不再作为唯一接口文档。
- 审计现有工程草案，确认保留或删除项。
- 完成条件：G0-A、G0-B 均通过；不要求数据库或运行功能。

### P1：样表分析与数据模型

- 输入：脱敏商品资料表、脱敏每日销售汇总表。
- 输出：源表分析、字段字典、ER 图、映射表、索引清单、建表 SQL 评审稿。
- 页面：静态字段映射评审原型，不连接数据库。
- 接口：无正式业务接口。
- 完成条件：G1 通过，无未决核心字段。

### P2：工程骨架

- 后端：配置分层、统一响应、异常、分页、OpenAPI、健康检查；按 G1 创建最小 `V1`。
- 接口：API-001 `GET /actuator/health`；API-002 `GET /api/v1/system/info`。
- 前端：布局、路由、请求封装、全局错误、403/404、系统状态页。
- 验收：浏览器显示真实后端健康状态，后端启动测试和前端构建通过。

### P3：登录、用户与门店

- 接口：API-010 至 API-024，涵盖登录、退出、当前用户、用户管理和门店管理。
- 页面：登录、用户管理、门店管理、角色菜单。
- 权限：为两种角色返回明确的菜单与操作权限；普通用户包含 `USER_CATALOG` 和 `SALES_IMPORT`，可查看所属门店最新已知库存，但不包含库存管理、经营分析、成本利润和 AI 权限。
- 验收：页面创建普通用户并登录；普通用户不能切店，不能访问未授权管理员接口，但可以进入所属门店的每日销售导入页；越权返回 403。

### P4：商品与基础资料

- 接口：API-030 至 API-056，涵盖公开商品查询、管理员商品、门店售价、商品供应商关系、供应商、分类、仓库货位、商品货位和 ES 索引管理。
- 条码格式：按字符串处理并保留前导零，只允许 1 至 64 位数字或英文字母；Excel 科学计数法导致的精度丢失不能自动修复，预检时按错误行阻断。
- 商品修改：API-035 允许管理员修改条形码；新条码必须通过格式和全局唯一校验，商品内部 ID、历史销售、门店商品、库存快照、供应商和货位关系不迁移也不重建。商品资料导入以当前条码作为匹配键，不承担条码修改；不建立旧条码别名或历史条码匹配机制。
- 商品单位：直接保存为自由文本，不建设单位字典及单位管理接口；API-034/API-035 允许管理员维护，API-030/API-031 向普通用户只读展示。
- 供应商进价：商品—供应商关系只保存最新进价，管理员可查看，普通用户只看到供应商名称；不建设进价历史，历史销售成本以每日销售事实的成本快照为准。供应商及报价变化不进入业务审计。
- 售价归属：当前售价属于门店商品，不属于全局商品主档；同一条码在不同门店可有不同当前售价。普通用户只读取所属门店售价，管理员可查看并维护各门店售价；历史销售中的售价继续作为当日事实快照保存。
- 管理员手工修改售价时页面不要求填写日期；系统自动以 `Asia/Shanghai` 的操作当天作为售价来源日期，并单独记录精确操作时间。同一天内按最后一次成功的修改或导入结果为准。
- 页面：普通商品目录展示所属门店当前售价、最新已知库存和最后同步时间；管理员商品页展示并可维护各门店售价，同时展示各门店库存；另有基础资料页和货位关联页。
- 验收：前导零往返不丢失，1 位和 64 位数字/字母条码通过，空值、65 位、空格、符号和精度已丢失的科学计数法条码被拒绝；管理员可以从商品编辑页修改为未占用的合法条码，冲突条码被拒绝，修改后 ES 可按新条码检索且既有业务历史仍归属同一商品；普通用户页面无编辑入口，直接调用修改接口返回 403。普通用户只能得到所属门店售价和库存，响应不存在其他门店售价、其他门店库存、进价、销量、金额、成本、利润或毛利字段；管理员无需填写日期即可修改任一门店商品售价，系统自动记录来源日期和操作时间，修改一家门店售价不影响另一家门店；索引暂时不可用时返回明确错误且不泄露敏感字段。

### P5：Excel 映射与预检

- 接口：API-060 至 API-073，涵盖映射模板、目标字段、上传预检、批次、提交、放弃和错误下载。
- 状态：`UPLOADED → VALIDATING → READY → COMMITTED`；失败 `INVALID`，放弃 `DISCARDED`。
- 页面：管理员使用“文件、门店与日期 → 模式与映射 → 预检 → 提交”四步向导；商品资料导入必选资料日期，每日销售导入必选营业日期。普通用户的每日销售向导固定所属门店。
- 权限：普通用户仅能执行 `DAILY_SALES`，不能执行商品资料或库存同步导入，也不能维护字段映射模板。
- 模板：普通用户可选择管理员已启用的每日销售映射模板；模板的新建、修改和停用仍仅限管理员。
- 字段取舍：商品资料中的“毛利率”不提供目标字段，不写入任何核心实体；每日销售中的“销售毛利率”作为销售事实快照保留，但同期销售收入、同期销售数量和同期销售毛利率不提供目标字段。自动映射或管理员映射均不能绕过目标字段白名单，舍弃列在预检中展示为忽略列。
- 验收：支持 `.xls/.xlsx`、列乱序、别名、前导零、重复键、多余列；商品资料毛利率和销售表三列同期值显示为忽略列，当期销售毛利率正确映射为历史快照；错误行阻断整批。

### P6：商品资料与 POP 库存同步

- `PRODUCT_ONLY`：按所选门店和资料日期 upsert 商品及门店售价，不改库存。
- 业务前提：POP 已有完整商品和库存数据；这里的“新增商品”仅指该商品尚未同步到本系统，不代表尚未进入 POP、尚未采购或尚未完成实体入库。
- `PRODUCT_INVENTORY_SYNC`：POP 可导出单门店全部商品并包含零库存、已停用商品，但新系统允许拆成多份逐批同步；每批按条码 upsert，新系统中不存在的条码新增商品主档和门店商品关系，已有条码更新资料并覆盖该店库存。
- 商品资料导入前由管理员必选资料日期，代表该文件的 POP 导出数据日期；不从文件名、上传时间或提交时间推断。
- 商品资料中的“售价”映射为所选门店的当前售价，不写入全局商品主档；资料日期不早于现有售价来源日期时才允许覆盖，并同步更新售价来源日期，同一商品在其他门店的售价不受影响。
- 商品资料中的“库存数量”同样以资料日期作为库存来源日期；旧资料可以保留导入批次和预检记录，但不得覆盖来源日期更新的门店库存。
- 商品资料中的“毛利率”始终舍弃，不写入商品主档、门店商品或商品供应商关系，也不用于覆盖或计算当前售价、当前进价。
- 已有条码按“非空覆盖”合并：文件字段为空时保留系统旧值，非空且校验通过时才覆盖；空值不表示清空。新商品缺少条码、名称等必填字段仍属于错误行，非空字段格式错误也不能因旧值存在而跳过校验。
- 不提供 `INBOUND` 累加和人工库存调整，避免产生第二套库存口径。
- 接口：API-080 至 API-087，门店最新已知库存、总库汇总、快照、同步记录、新鲜度和告警。
- 页面：管理员商品库存、总库最新已知库存、快照历史、同步记录、陈旧数据和负库存告警。
- 验收：未选择资料日期时不能预检；已有商品主档基础上，可用小批次同步 POP 中已有但尚未进入本系统的商品；支持将商品资料拆成多批依次导入；已有商品空字段保留旧值、有效非空字段覆盖、新商品必填字段缺失阻断；较新或同日资料可更新本店售价和库存，旧资料不回退现值；门店隔离、同条码新增/更新、批次未出现商品保持不变、来源优先级、汇总准确和事务失败整批回滚；普通用户能从商品目录看到本店最新库存但不能切换门店。

### P7：每日销售导入

- 业务键：门店 + 营业日期 + 商品。
- 营业日期由管理员或普通用户在上传预检前从页面选择，销售文件内不解析日期；一次文件只归属一个营业日期。
- 管理员选择目标门店；普通用户不选择也不能提交 `storeId`，后端强制使用账号绑定门店。
- POP 字段中的“当前机构”就是本次导入的目标门店，不允许将该字段解释为其他门店或全局机构。
- 先过滤“本期销售数量 = 0”的源行：整行舍弃，不创建销售事实，也不参与门店售价、库存同步或同条码聚合；即使该行销售收入非 0 也同样舍弃。负销量不属于此规则，仍作为退货事实。
- 业务前提：每份每日销售文件在过滤零销量行后至少保留一条非零销量数据；“整份文件均为零销量”不纳入首版功能分支、错误码和验收范围。
- 同文件同条码的不同供应商行在零销量行过滤后按条码聚合，供应商不进入核心销售事实。
- 销售汇总只包含当日销售商品，属于 POP 部分库存快照；只覆盖文件中出现商品的门店库存，未出现商品保持原值。
- 销售数量不加减库存；修正版只覆盖销售事实，负销量只作为退货事实。库存按文件中的“选中机构库存数量/当前机构库存数量”直接覆盖。
- “当前机构售价”始终保存为该营业日期的销售快照；当营业日期大于或等于该门店商品现有售价来源日期时，同时覆盖该门店当前售价并更新售价来源日期。营业日期更早时只更新销售事实，门店当前售价保持不变；同日修正版以最后成功提交批次为准。
- 每日销售表中的“销售毛利率”按门店、营业日期和商品保存为历史事实快照，不使用商品资料毛利率或后续变更的售价、进价覆盖。
- 每日销售表中的同期销售收入、同期销售数量和同期销售毛利率全部舍弃，不写入销售事实，也不用于补齐历史比较结果。
- 较旧营业日期的补录只更新销售事实，不允许覆盖来源日期更新的库存；同日修正版以最后成功提交批次为准。
- 接口：复用 API-065 至 API-073；预检和批次详情增加零销量忽略数量及行级忽略原因，并展示营业日期、销售新增/覆盖/退货、库存覆盖/跳过、售价覆盖/跳过和陈旧风险摘要。
- 页面：管理员与普通用户共用销售导入页，根据角色固定/选择门店；页面包含营业日期、文件上传、预检结果和确认提交。
- 数据隔离：普通用户可以查看所属门店全部用户创建的销售导入批次，但不能查看其他门店批次；敏感成本、毛利及经营明细不在普通用户响应中返回。
- 操作范围：普通用户只能提交或放弃本人创建且尚未提交的批次；管理员可处理全部门店批次。
- 验收：两种角色均可从页面完成销售导入；普通用户能看到所属门店其他用户创建的批次，但伪造 `storeId`、访问其他门店批次、操作他人批次或导入其他类型时返回 403；零销量行不产生销售事实且不更新售价或库存，负销量仍保留为退货事实；直接覆盖、缺失不清零、重复条码一致性、旧日期跳过库存与售价、同日修正版覆盖售价、修正版销售、负库存告警和事务回滚测试通过。
- 销售事实长期保留，首版不设置自动删除或只保留最近一个月的规则；用户每次选择的主查询区间最多连续 31 个自然日。
- 同比、环比所需的历史对比区间由后端根据主查询区间自动计算和读取，不要求用户扩大主查询窗口，也不受“只能保留一个月”限制。

### P8：经营看板

- 接口：API-090 至 API-095，概览、趋势、门店对比、商品排行、毛利、异常。
- 页面：统一门店/日期筛选、指标卡、趋势、门店对比、排行、毛利和异常。
- 比较口径：同比取主查询区间向前推一个自然年的对应日期区间；环比取紧邻主查询区间之前、自然日数量相同的连续区间。全部从系统销售事实计算，不读取 POP 同期列；历史数据不足时返回“暂无可比数据”及缺失原因，不把缺失误报为增长率 0%。
- 验收：G4 通过；金额精度、退货、空周期、跨月、门店过滤、普通用户 403 测试通过。

### P9：冷热商品

- 默认权重：销量 35%、销售金额 30%、增长率 20%、动销天数 15%；合计 100%。
- 默认阈值：热门 80、冷门 30；结果保存配置快照。
- 接口：API-100 至 API-104，评分配置、运行和结果。
- 页面：评分配置、冷热列表、维度得分和解释。
- 验收：权重、相同极值、无上期、新商品、零销售和快照稳定性测试通过。

### P10：固定多 Agent

- 流程：主管校验 → 数据质量/销售/商品/库存四专家并行 → 报告 Agent 汇总。
- 接口：API-110 至 API-116，会话、运行、SSE 和报告。
- SSE：`task_started`、`agent_started`、`stage_result`、`chart`、`report`、`completed`、`error`。
- 页面：会话、提问、Agent 时间线、流式答案、受控图表、历史报告。
- 验收：模型桩验证编排和失败；真实 Qwen 受控联调；普通用户 403。

### P11：审计、验收与上线

- 接口：API-120 至 API-122，审计日志、配置状态和运维状态。
- 审计定义：审计日志是关键业务操作的只读追踪记录，不是销售事实、库存快照或程序错误日志；记录操作人、时间、模块、动作、目标对象、门店、执行结果、请求追踪号及必要的变更摘要。
- 审计范围：记录登录及失败、账号权限、导入预检/提交/放弃、商品条码修改、门店售价手工修改、库存同步与告警处理、分析配置与 Agent 操作；供应商主档及商品供应商关系不纳入业务审计。
- 敏感保护：不记录密码、JWT、Redis 会话值、DashScope/API Key、数据库凭据或完整 Excel 内容；审计日志仅管理员可查，业务页面不提供编辑或删除能力。
- 页面：审计日志与系统状态。
- 验收：浏览器完成管理员“导入 → 库存 → 销售 → 冷热 → AI”闭环；关键操作可按操作人、模块、动作、门店、结果和时间检索，普通用户访问审计接口返回 403，敏感值不进入审计；普通用户只能查看所属门店公开商品字段及最新已知库存，并完成所属门店的每日销售导入，不能进入库存管理、经营数据与 AI 页面。
- 输出：部署、备份恢复、环境变量、阿里云切换和管理员操作手册。

## 8. 测试与完成定义

| 层级 | 工具 | 覆盖 |
|---|---|---|
| 领域单元 | JUnit 5 | POP 库存覆盖、部分快照、乱序保护、销售修正、退货事实、评分 |
| 后端集成 | Spring Boot Test、Testcontainers | MySQL 约束、事务、权限、Redis |
| 前端单元 | Vitest、Vue Test Utils | 表单、字段隐藏、图表转换 |
| 页面联调 | Playwright | 每个节点通过真实接口走主流程 |
| AI 合同 | 固定模型桩 | 编排、SSE、结构化输出、失败恢复 |

节点完成条件：接口与页面全部完成；页面不用模拟数据；测试通过；OpenAPI、字段字典和进度文档同步；用户验收后才开始下一节点。

## 9. 当前状态

- 当前停留 P0，先等待项目计划评审；计划定稿后再生成 M01–M10 独立接口文档。
- 未启动、创建或修改数据库，提前生成的 SQL 已删除。
- 已有代码是未评审工程草案，G0 通过后先审计，不视为正式实现。
- P1 开始条件是提供两类脱敏 Excel 样表。
