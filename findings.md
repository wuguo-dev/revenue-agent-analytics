# 实施发现

- M01 当前尚无独立接口文件；总体计划指定目标为 `docs/api/01-system-auth.md`，现有基线仅分散在 `docs/api-contract.md` 与 `docs/implementation-plan.md`。
- M01 现有接口范围为 API-001 健康检查、API-002 系统信息、API-010 登录、API-011 退出、API-012 当前用户；用户管理 API-013～018 与门店管理 API-020～025 按总体模块划分不属于 M01，应由后续模块独立评审。
- 现有认证基线使用 Bearer access token；退出时把当前令牌加入 Redis 失效列表，管理员重置密码后现有令牌全部失效；尚未明确 access token 有效期、是否提供 refresh token、并发会话策略、令牌传输与存储细节。
- 健康检查为公开的 Actuator 原路径且只返回 `status`；系统信息接口需登录，当前草案返回 `version, environment, serverTime, aiEnabled`，其字段暴露与两角色差异尚待确认。
- 总体计划引用的 `docs/api/00-conventions.md` 实际不存在，M01 定稿需避免依赖未落地的全局约定，并标记后续补齐。
- 总体模块边界已明确：M01 只负责系统基础读取与认证会话；用户、角色、账号门店绑定和门店管理属于 M02。M01 可以读取 M02 提供的用户状态/角色/门店信息，但不能承接账号管理接口。
- P2 工程骨架承载 API-001/API-002 和系统状态页，P3 承载登录页面与 M01/M02 联调；因此 M01 文档必须同时给出“未登录健康检查”和“登录后系统信息/会话”验收，但不把开发阶段划分误写为模块职责。
- 业务审计要求登录及登录失败进入 M10 审计，但敏感字段、密码、JWT、Redis 会话值和完整请求体禁止进入审计；M01 应只向 M10 发送脱敏认证事件摘要。
- 数据字典目前没有独立的用户会话或令牌模型字段定义，令牌策略必须在 M01 定稿后再进入 G1 数据模型评审。
- M01 应作为深模块提供小而稳定的外部 interface：登录、退出、解析当前会话、读取系统公开/受保护状态；密码校验、失败计数、令牌签发与轮换、Redis 会话/失效状态、账号状态复核和审计事件拼装均封装在模块实现内，不能让控制器、前端或 M02/M10 调用方编排。
- M01 与 M02 的 seam 是只读的认证主体查询/版本信息，以及 M02 在账号停用、角色/门店变更、密码重置后触发“使该账号所有会话失效”的内部命令；M01 与 M10 的 seam 是脱敏认证审计事件。Redis、JWT 库和时钟属于 M01 内部 seam，可分别提供生产 adapter 与测试 adapter，不暴露为业务接口。
- 现有前端只是未评审骨架：把 access token 写入 `localStorage`，只检查本地是否存在 token，未调用 `/auth/me` 校验会话，401 也不会清理状态或带返回地址跳转登录。M01 定稿后应替换这一行为，不能把早期草案当成约束。
- 当前登录页只有用户名/密码输入且按钮禁用；主布局菜单为硬编码，尚未使用后端权限。M01 的 `SessionView`/`/auth/me` 需要给出稳定的权限 code 列表，让前端基于服务端主体生成菜单并仍由后端独立执行授权。
- 本地编排已经包含 Redis，适合承载会话或令牌撤销状态；具体是纯会话、单 access token 还是 access+refresh token 仍需用户确认。
- OWASP 当前会话管理指南明确不建议把认证 token、JWT、refresh token 或 session ID 放入 `localStorage/sessionStorage`，优先使用带 `HttpOnly; Secure; SameSite` 的 Cookie 或 BFF；因此现有前端 `localStorage` 方案不应进入 M01 定稿。
- 对本项目这种同一组织自用、单一 Vue Web 客户端、后端和 Redis 均自控的场景，推荐使用 Redis 服务端会话 + 不透明 HttpOnly Cookie：即时退出/停用/密码重置更直接，interface 比 access+refresh JWT 更小。Cookie 方案同时必须落实 CSRF 令牌、Origin 校验和生产 HTTPS。
- Spring Security 官方资料建议密码使用自适应单向函数（bcrypt/PBKDF2/scrypt/Argon2）并按实际环境调节工作因子；M01 推荐 `DelegatingPasswordEncoder` + bcrypt 起步，具体工作因子在性能验收时校准，不把明文或可逆密文写入任何存储/日志。
- OWASP 建议登录失败统一通用消息并实施登录节流；现有“用户名或密码错误”基线正确，但仍需确认失败阈值、锁定范围和持续时间。
- 现有 M04 的逐步确认格式可复用到 M01；M01 建议拆为 8 步：范围/seam、会话模型、登录与防爆破、失效与并发、主体/权限响应、系统接口、错误/审计/安全、页面/测试/完成定义。
- 并行存在的 M02 草案与 M01 第 1 步的职责基本一致：M02 保存账号、角色、状态、密码哈希和门店绑定的权威数据；M01 执行登录、主体构造和会话失效。M02 当前仍使用“令牌/黑名单”泛称且未确认，待 M01 第 2 步确定 Cookie 服务端会话或其他模型后需要同步替换术语。
- M02 草案新增 API-019 个人修改密码；M01 不应吞并该 HTTP 接口，但应提供密码策略/编码和全会话失效的内部 interface，避免 M02 复制安全实现。
- M01 第 1 步已于 2026-09-12 获得用户明确确认：M01 负责系统读取、认证主体与会话；M02 负责账号/角色/门店权威数据；M10 负责审计持久化与查询；业务模块继续独立执行授权和门店隔离。
- M01 第 2 步已于 2026-09-12 获得用户明确确认：采用 Redis 服务端会话、不透明 HttpOnly Cookie、CSRF token + Origin 校验；空闲 30 分钟滑动续期、绝对 12 小时，浏览器重启后在有效期内可恢复，不建设“记住我”。
- OWASP 当前密码存储建议新系统优先 Argon2id（基线 19 MiB、迭代 2、并行度 1），bcrypt 更适合兼容遗留且存在 72 字节输入边界；因此 M01 第 3 步推荐改为 Argon2id，并通过 `DelegatingPasswordEncoder` 保留未来算法升级能力。
- 当前 M01 首版明确不做 MFA；依据 OWASP/NIST 的无 MFA 建议，密码最短推荐 15 个字符，最大至少 64 个字符且不采用字符组合规则。草案采用 15～128 Unicode 字符、本地常见密码拒绝清单、不周期性强制改密。
- 用户明确账号均由管理员创建并分发给员工，要求登录校验和密码安全保持简单；M01 第 3 步因此撤销 Argon2id、15～128 位和本地弱密码库的增强方案，改为 bcrypt、8～32 位、不强制字符组合、不周期改密、不强制首次改密，保留统一失败响应和临时登录冷却。
- 用户明确员工没有修改密码权限；员工忘记或需要更换密码时只能联系管理员重置。M02 不再提供 API-019 个人改密接口，也不建设个人密码页面；管理员是唯一可以设置或重置任何账号密码的角色。
- M01 第 3 步已于 2026-09-12 获得用户明确确认：管理员统一创建、分发和重置密码，员工无改密权限；密码采用 bcrypt 哈希、8～32 位半角字符、不强制复杂组合，同一用户名连续失败 5 次后冷却 15 分钟。
- M01 第 4 步已于 2026-09-12 获得用户明确确认：每账号最多 3 个并发会话，第 4 次登录撤销最早会话；当前退出幂等；管理员重置密码、取消账号登录资格、改角色或改门店后全部旧会话失效；Redis 故障不绕过认证。
- M01 第 5 步推荐基于固定角色返回 5 个稳定权限 code，而不是拆出十余个管理员细粒度 code；`ADMIN` 汇总全部管理员能力，普通用户只有系统读取、商品目录、分类组织和本店销售导入，菜单结构仍由前端静态维护。
- M01 第 5 步已于 2026-09-12 获得用户明确确认：主体返回固定角色、普通用户唯一门店、会话时间和 5 个权限 code；不建设动态权限或服务端菜单结构，前端裁剪不替代后端鉴权。
- API-011 若强制 `AUTHENTICATED` 会与“失效会话重复退出仍成功”的幂等语义冲突；M01 第 6 步草案已改为 `PUBLIC + CSRF`，无有效会话时仍清理 Cookie 并返回成功。
- M01 第 6 步已于 2026-09-12 获得用户明确确认：API-001/002/009～012 的路径、权限和响应采用草案定义；API-001 是机器健康探针，以 Java 后端、MySQL、Redis 为核心健康项，ES/AI 异常不把整体状态判为 DOWN。
- M01 第 7 步已于 2026-09-13 获得用户明确确认：认证错误、审计事件、敏感字段排除、HTTPS、缓存控制、安全响应头和同站点/CORS 策略采用第 9 节定义。
- HttpOnly 会话 Cookie 不能被前端 JavaScript 删除；因此退出请求网络失败时不能只清内存并跳登录，否则页面刷新可能再次恢复旧会话。M01 第 8 步改为保留登录状态并提示重试，服务端成功后再清理和广播。
- M01 第 8 步已于 2026-09-13 获得用户明确确认，8 个步骤现已全部通过；定稿收尾必须把总接口稿中遗留的 Bearer access token、Redis 令牌黑名单、`accessToken` 响应、环境/AI 状态字段和 JWT 敏感字段表述替换为已确认的 Redis 服务端会话 + Cookie 模型。
- M01 定稿需要同步的主要文件为 `docs/api-contract.md`、`docs/implementation-plan.md`、`docs/data-dictionary.md`、`README.md`、`CONTEXT.md` 和 M02 关联口径；M02 当前仍在独立逐步评审，必须只同步已由 M01 定稿决定的认证约束，不改变其当前确认步骤或未确认业务结论。
- M01 第 6 步最终健康语义为：API-001 只以 Java 后端、MySQL、Redis 为核心健康项；Elasticsearch/AI 故障不令整体 health DOWN，详细组件状态留给 M10。

- 仓库初始化时为空，仅存在 `.git`，没有兼容性负担。
- 技术栈固定为 Java 17 / Spring Boot 3.5.8 / Spring AI Alibaba 1.1.2.2 / MyBatis-Plus 3.5.17 与 Vue 3 / TypeScript。
- 两家门店库存相互隔离；总库存是门店库存求和结果。
- 仓库和货位只描述位置，商品可关联多个货位，不保存货位库存。
- 销售文件中的进价、售价、毛利率和金额是历史事实快照。
- 普通用户绑定一个门店，可读取商品公开字段、所属门店最新已知库存并为该门店导入每日销售汇总，但不能查看其他门店库存、查询销售成本利润等经营数据或使用 AI。
- 用户最初明确要求商品搜索使用 Elasticsearch；MySQL 为权威数据源，ES 仅承担商品检索。
- 已收到两份 POP 参考文件：商品资料为 20,480 字节，商品销售汇总为 235,520 字节，格式均为旧版 `.xls`。
- 当前捆绑 Python 环境支持 pandas/openpyxl，但缺少读取 `.xls` 的 xlrd；需要使用只读兼容解析器。
- 商品资料样表实际有 1 条记录、12 个字段：商品名称、条码、单位、供应商名称、含税成本价、售价、毛利率、品类编码、品类名称、库存数量、商品备注、提成率/固定值。
- 销售汇总包含 899 条商品明细、1 条合计行、15 个字段；有效条码均为 13 位文本。
- 销售汇总有 892 个唯一条码、7 个重复条码组（14 行）。重复组的名称、品类、进价、售价、库存一致，供应商均不同，说明商品与供应商可能是多对多。
- 销售明细合计为本期数量 988、收入 7342；同期数量 738、同期收入 6670.8。销售占比合计约 100%，属于可计算字段；日均销售列全部为 0。
- 68 条明细的销售收入不等于销售数量乘当前售价，表明存在折扣/成交价差；实际销售收入不能由当前售价重算。
- 销售文件没有营业日期列；用户已确认由管理员或普通用户在导入页面选择营业日期。
- 销售表的库存列是 POP 权威库存；用户已确认直接覆盖文件中对应门店商品的最新已知库存。
- 用户确认商品与供应商为多对多，不需要供应商维度的销量、销售额或毛利分析；供应商用于展示商品来源。
- 不同供应商可在不同日期为同条码商品供货且进价不同，因此采购价格不能只是商品主档上的单一值。
- 每日销售导入按唯一条码聚合；销量和退货不再加减库存，库存直接使用 POP 字段覆盖。销售事实长期保留且首版不自动删除，用户单次选择的主查询区间最多连续 31 个自然日。
- 普通用户的销售导入请求不能自行指定门店，后端必须从账号绑定门店取值；管理员导入时选择门店。
- 普通用户可查看所属门店全部用户创建的销售导入批次；“看到”不扩大写权限，普通用户仅可提交或放弃本人创建的未提交批次。
- 数据库字段尚未确认；必须在取得真实脱敏 Excel 样表并逐表评审后才生成迁移。
- 库存来源方案已确认：POP 是唯一权威来源，销售事实不再加减库存；系统保存最新已知库存、快照、同步差异、来源批次、来源营业日期和同步时间。
- 用户确认 POP 每日销售汇总只包含当日销售商品，因此它是部分快照；文件未出现商品保留上次库存，不能自动清零。样表中的零销量行整行舍弃，不属于可提交的部分库存快照。
- 用户补充商品资料表同样包含 POP 最新库存；管理员可重复导入商品资料，直接覆盖文件中商品的旧库存，用于刷新未销售商品，不做进货累加。
- 用户确认普通用户可以在商品资料页面查看所属门店最新已知库存，但不能查看其他门店库存或其他敏感经营字段。
- 用户澄清 POP 商品资料的可导出范围是单门店全部商品并包含零库存、已停用商品，但系统允许将商品资料拆成多份分批导入；因此 `PRODUCT_INVENTORY_SYNC` 必须按批次局部覆盖，不能做全量缺失判断。
- 用户举例确认增量场景：POP 中商品和库存数据已经完整；已有本系统商品主档后，可将 POP 中已有但尚未同步到本系统的商品组成小批次导入。新系统中无此条码则新增、已有条码则更新，其他商品不受影响；该场景不是采购或实体入库。
- 用户确认商品资料增量更新采用非空覆盖：已有条码的空字段保留系统旧值，不作为清空指令；新商品必填字段缺失和非空值格式错误仍需预检阻断。
- 用户确认商品条形码允许管理员修改，普通员工不允许修改。条码只按当前值保证全局唯一，内部商品 ID 保持历史关联；不考虑旧条码别名，导入仅匹配当前条码。
- 用户接受条码格式方案：按字符串保存并保留前导零，允许数字和英文字母，最大长度 64；据此采用 1–64 位字母数字校验，不接受空格或其他符号。
- 用户确认同一条码的当前售价按门店分别保存；普通用户只查看账号所属门店售价，管理员可查看并维护各门店售价。当前售价归属门店商品关系，不能作为全局商品唯一字段。
- 用户确认每日销售表中的“当前机构”就是本次导入的目标门店；“当前机构售价”用于更新该门店当前售价，但只有营业日期不早于系统现有售价来源日期时才覆盖。较旧日期仍保存销售事实和售价快照，不回退当前售价。
- 用户接受商品资料导入前由管理员必选资料日期，代表 POP 导出数据日期；该日期作为批次内门店售价和库存的来源日期，旧资料不得回退来源日期更新的售价或库存。
- 用户确认管理员手工修改门店售价时不填写任何日期；系统自动以操作当天作为售价来源日期，并另行记录精确操作时间。API-039 只提交售价和并发版本。
- 用户接受长期保留全部每日销售事实、首版不自动删除；31 天仅限制用户单次选择的主查询窗口，同比和环比由后端自动读取对应历史区间。
- 用户接受商品—供应商关系只保存最新进价、不建设历次进价历史；历史销售成本以每日销售事实快照为准，普通用户不能查看进价，供应商及报价变化不进入审计日志。
- 2026-09-13 用户确认 M03 门店商品供应商生命周期：不设置供应关系或供应商启用/停用状态，不提供 API-038 或其他人工关系/进价写入；全部数据变更由 POP 导入直接按“门店 + 商品 + 供应商”新增或覆盖，非空新供应商名称无需预建，后续批次未出现的旧记录保留。普通用户查看所属门店全部供应商名称。
- 业务审计日志定义为系统自动生成的关键业务操作追踪记录，区别于销售事实和程序运行日志；仅管理员可查，不记录凭据、令牌、完整请求或完整 Excel 内容。
- 用户接受舍弃商品资料中的毛利率，不写入商品核心数据或导入目标字段；历史毛利分析只使用每日销售事实中的销售毛利率快照。
- 用户接受舍弃每日销售表中的同期销售收入、同期销售数量和同期销售毛利率；同比取去年对应日期区间，环比取紧邻主查询区间之前的等长区间，全部从系统历史事实计算，历史不足时不使用 POP 值补齐。
- 用户确认每日销售表中“本期销售数量 = 0”的行整行舍弃：不创建销售事实，不更新门店售价或库存，即使销售收入非 0 也一样；负销量仍作为退货事实。
- 用户确认不会出现整份每日销售文件在过滤零销量行后无任何非零销量数据的情况；首版不设计该情形的专用错误码、提交分支或验收场景。
- 用户接受商品单位直接保存为自由文本、不建设单位字典；管理员可修改，普通用户只读，商品资料导入沿用非空覆盖规则。
- 用户不接受仅一级分类，要求分类全系统共享并支持多级层级。商品资料导入时带入商品类别；普通用户和管理员都可以创建父级分类，并把导入类别归入父级目录。现有 POP 样表无法表达父级，后续需确认新类别初始父级及重复导入是否保留人工归类。
- 用户接受分类导入时序规则：系统中首次出现的 POP 商品类别创建为根分类；员工完成父级归类后，后续商品资料重复导入保留该人工父级，不得重置分类树。
- 用户确认同一 POP 品类编码在后续批次名称变化时采用覆盖：提交后更新该 POP 分类名称，但保留人工父级；人工父级没有 POP 编码，不受导入名称覆盖。
- 用户进一步确认 POP 分类名称覆盖不受商品资料日期限制：即使补录较早日期文件，也按成功提交顺序以本批名称覆盖当前名称；售价和库存仍沿用来源日期防回退规则。
- 用户接受分类树最大 5 级，根分类计为第 1 级；创建或移动分类时必须校验最终层级，移动含子分类的节点还要校验整个子树，不能让任何节点进入第 6 级。
- 用户接受商品只能关联叶子分类：任何分类不能同时直接挂商品和拥有子分类；目标父分类已有商品时禁止创建或移入子分类，商品资料导入匹配到非叶子 POP 类别时整批阻断。
- 用户确认管理员可以手工修改商品所属分类，普通用户不能修改；目标仍必须是启用的叶子分类，变更应记录审计。后续 POP 商品资料对手工分类的覆盖优先级待确认。
- 用户接受管理员手工商品分类优先于 POP：商品资料再次导入不同类别时，预检展示差异但不阻断，提交后保留手工分类；需要保存 `MANUAL_OVERRIDE` 来源状态以区别普通 POP 归属。
- 用户接受提供“恢复使用 POP 分类”：系统需同时保存当前有效分类和最近一次 POP 分类；管理员恢复后当前分类指向最近 POP 分类、来源改回 `POP_IMPORT`，后续导入重新自动更新。
- 用户接受分类停用不级联商品：管理员可以停用已有商品关联的分类，商品保持原状态和分类并继续可查询，页面显示停用标记；停用分类不能用于新的商品归属或恢复 POP 分类。
- 用户接受分类维护权限：普通员工可创建分类和调整父级，但分类重命名、启用及停用仅管理员可操作。
- 用户接受分类标识规则：POP 导入类别以品类编码作为全系统唯一匹配键；员工手工创建父级时只填写名称、不设置业务编码，由系统内部 ID 标识。
- 用户约定先完善并定稿总体项目计划；定稿后再按 M01–M10 拆分业务模块，每个模块单独生成完整接口文档，当前总接口稿仅作清单。
- 当前 `main` 分支已有三次历史提交，`origin` 指向目标 GitHub 仓库，但本地分支尚未显示上游跟踪关系。
- 当前 HEAD 已删除未评审的后端骨架和 `frontend/index.html`，仓库不是可运行项目；旧 README 的本地启动说明已失效。
- GitHub CLI 中 `wuguo-dev` 的已保存令牌失效；项目文件扫描未发现 GitHub、OpenAI 或 DashScope 实际密钥。
- 目标仓库公开后可匿名读取；远程 `main` 只有一个独立的 `Initial commit` 和两行 README，与本地历史没有共同祖先。
- 远程 README 提到每日日报、周报和库存警戒通知；该愿景已合并进新版 README，但具体频率、阈值、通知渠道和首版范围仍需业务评审。
- 本机已有 SSH 密钥且成功认证为 GitHub 账号 `wuguo-dev`；`origin` 已切换为 `git@github.com:wuguo-dev/revenue-agent-analytics.git`。
- 用户最新规则整体替换此前“手工分类优先 + 最近 POP 分类 + 恢复 POP 分类”模型：商品只保存一个当前分类，后续导入不再覆盖已有商品分类，也不再保存最近 POP 分类或提供恢复接口；此前相关结论视为已废止。
- 用户明确首版只有【商品资料】和【每日销售汇总】两种导入模板，不再为商品资料拆分独立业务模式；两种导入均可更新库存、当前进价和门店售价，并继续使用来源日期防止旧数据回退。
- 用户确认“第一次初始化导入”按每个条码首次进入本系统判定，不是仅指全系统第一个商品资料批次；因此后续小批量新增商品仍可取得 POP 初始分类，已有商品分类不再被导入覆盖。
- 用户最终确认当前进价的保存粒度为“门店 + 商品 + 供应商”，而不是“商品 + 供应商”；同一商品和供应商在不同门店可以有不同当前进价，任一导入只更新目标门店组合，旧的商品级关系表述视为已废止。
- 用户纠正 POP 分类不存在停用场景，最多出现同一条码后续携带不同品类；商品分类以该条码首次导入形成的分类为准，后续导入不覆盖，管理员仍可在系统中修改商品分类。导入不再保留“新商品命中已停用 POP 分类”的待确认项或处理分支。
- 当前总体接口清单已经为 M04 预留 API-060 至 API-073，范围包括导入模板、目标字段、上传预检、批次查询、行明细、提交、放弃和错误下载；独立模块文件 `docs/api/04-imports.md` 尚不存在。
- M04 粒度规划必须复用当前已确认约束：仅有 `PRODUCT` 与 `DAILY_SALES` 两类导入；商品资料仅管理员可导入且必选门店和资料日期；每日销售普通用户固定所属门店且必选营业日期；有错误整批不可提交；普通用户门店内可读但仅能操作本人未提交批次。
- 总接口稿当前把“文件上传、表头解析、字段映射确认、业务预检”合并在 API-067 `POST /imports/preview`；这无法自然支撑四步向导中的“先读取真实表头、再人工修正映射、最后运行预检”。M04 独立稿应把上传解析与预检拆成两个操作，并相应扩展接口编号，避免依赖浏览器自行解析旧版 `.xls`。
- 总接口稿提到模板“已启用”选项，但模板请求尚未明确状态字段；M04 独立稿需要固定 `ENABLED/DISABLED` 生命周期、历史批次映射快照与删除限制。
- 样表确认商品资料 12 列、销售汇总 15 列；销售表包含合计行、重复条码、多供应商、零销量和负库存。M04 预检必须明确行识别顺序：工作表/表头检查 → 排除合计行 → 零销量过滤 → 基础字段校验 → 重复条码一致性/聚合 → 与系统现值比较 → 生成提交计划。
- M04 应设计成深模块：对前端只暴露模板、上传解析、预检、批次读取、提交/放弃和错误导出这些稳定接口；文件解析器、映射解析器、行校验器与导出器属于内部 seam，不应泄露成前端必须编排的碎片接口。
- M04 与 M03/M05/M06 的 seam 使用类型化预检/提交端口：M04 维护批次状态、文件与映射快照并编排事务，商品/分类、库存、销售模块提供业务计划生成与应用能力；测试通过同一模块接口使用内存 adapter，避免在导入模块复制各业务规则。
- M04 独立稿采用 API-060 至 API-075：API-068 只负责上传、文件级解析和真实表头，API-069 保存映射并预检，API-074 使用 `expectedVersion + previewToken` 原子提交，API-075 放弃未提交批次；总接口清单已同步该编号。
- 模板生命周期明确为 `ENABLED/DISABLED`，内置模板只读，自定义模板被批次引用后只能停用；每个批次保存最终映射快照，避免模板后续修改改变历史预检。
- M04 静态一致性检查通过：独立稿与总接口稿都包含且仅包含 API-060 至 API-075；有效文档不再引用旧的 `/imports/preview` 合并接口；本轮修改涉及的 Markdown 本地链接全部可解析。
- 用户要求所有规划必须逐步确认，不接受一次性交付后直接视为完成。M04 已拆为 10 个顺序评审步骤；只有当前步骤获得明确确认后才能进入下一步，全部确认前阶段保持 `in_progress`。
- M04 第 1 步“模块目标、范围与不做事项”于 2026-09-12 获得用户明确确认；进入第 2 步“角色权限与门店数据范围”评审。
- M04 第 2 步“角色权限与门店数据范围”于 2026-09-12 获得用户明确确认；管理员可处理全部门店批次，普通用户固定所属门店、同店销售批次可读但仅本人未提交批次可写，所有普通用户响应必须脱敏。进入第 3 步状态机评审。
- M04 第 3 步“批次枚举与状态机”于 2026-09-12 获得用户明确确认；采用 `UPLOADED → VALIDATING → READY/INVALID → COMMITTED` 主流程，`DISCARDED` 为放弃终态，提交失败回滚后保持 `READY`。进入第 4 步模板与目标字段评审。
- M04 第 4 步“模板生命周期与目标字段”于 2026-09-12 获得用户明确确认；内置模板只读，自定义模板支持启停且被引用后不可删除，两类导入目标字段、条件必填、校验专用字段和固定忽略列均确认。进入第 5 步字段映射评审。
- M04 第 5 步“字段映射优先级与映射校验”于 2026-09-12 获得用户明确确认，并采用解释后的简化方案：普通用户只选启用模板并使用自动映射，不能提交手工映射；只有管理员可以调整本批映射及保存自定义模板。进入第 6 步文件解析与预检顺序评审。
- 第 6 步评审时用户询问具体工作表表头。当前工作区未保留原始 `.xls/.xlsx` 文件，无法再次目视确认表头占一行还是两行；只能依据 `docs/source-field-review.md` 中已提取的 12 个商品资料字段和 15 个销售字段说明，带 `|` 的名称暂表示层级表头规范化路径。
- M04 第 6 步“文件上传、解析限制与预检顺序”于 2026-09-12 获得用户明确确认；工作表名称不固定，按规范化表头匹配，原始表头行布局保留为编码前样表复核项。进入第 7 步 API-060 至 API-075 评审。
- M04 第 7 步 API-060 至 API-075 于 2026-09-12 获得用户明确确认。API-069 的“保存映射”已澄清为“确认本批字段对应关系并保存批次快照”，不等于创建或修改映射模板。进入第 8 步两类业务预检规则评审。
- M05 当前只有 API-080 至 API-087 的粗粒度清单；库存快照与同步记录都按商品差异描述，职责存在重叠。独立规划应将同步记录收敛为“每个已提交导入批次的一条库存执行摘要”，将库存快照定义为“该批次每个门店商品的一次 POP 观察值”，避免重复保存两套行级明细。
- M05 新鲜度既要保留 `sourceDate` 用于阻止旧值回退，也要保留成功应用当前投影的 `lastSyncedAt` 用于衡量系统最近确认 POP 观察值的时间；旧日期跳过的快照不能刷新当前库存的新鲜度。
- M05 告警仍有三项需要业务确认：陈旧阈值、是否仅做站内告警、以及人工处理采用“确认知悉”还是允许在异常仍存在时直接关闭。
- 用户确认 M05 推荐方案，并将陈旧库存定时扫描时间从 08:00 调整为每天凌晨 04:00（Asia/Shanghai）；其余已确认项保持为全局 3 天阈值、`lastSyncedAt` 新鲜度基准、负库存/陈旧库存站内告警、人工确认知悉与系统自动恢复。
- M02 尚无独立接口文档；现有总接口稿预留 API-013 至 API-025。已明确两类角色、普通用户唯一绑定一个门店、管理员不绑定门店、用户名创建后不可修改、管理员不能停用当前登录账号；账号生命周期、门店停用影响和最终字段仍需逐步确认。
- M02 评审稿拆为 8 步：角色绑定、用户生命周期、用户名与密码、门店生命周期、门店字段、API/DTO/并发、seam/权限/审计、安全与验收定稿。当前只开放第 1 步，推荐固定 `ADMIN/USER`、管理员无门店、普通用户唯一绑定一个启用门店、一个门店可有多个普通用户、普通用户门店范围只取服务端绑定值。
- M02 第 1 步“角色模型与账号门店绑定基线”于 2026-09-12 获得用户明确确认：固定 `ADMIN/USER` 两种角色，管理员不绑定门店，普通用户唯一绑定一个启用门店，一个门店可绑定多个普通用户，普通用户门店范围只取服务端账号绑定。进入第 2 步用户生命周期评审。
- M02 第 2 步收到修改意见：取消用户 `ACTIVE/DISABLED` 状态，只保留可登录账户；管理员应能删除普通用户账户。评审稿据此改为不可恢复的账户删除草案，并提出保留最小历史身份、用户名永久保留、删除即令牌失效、管理员账户不可删除等配套规则，等待用户确认。
- M02 第 2 步新增修改意见：管理员可注册管理员账户与普通用户账户，管理员账户也支持注销。评审稿调整为管理员可删除普通用户、可注销自己或其他管理员，但最后一个管理员不可注销或转为普通用户；自助注销成功后立即退出，等待用户确认。
- M02 第 2 步“账户创建/注销、角色切换与门店改绑”于 2026-09-12 获得用户明确确认：无用户启停状态；管理员可创建两类账户、删除普通用户、注销自己或其他管理员；系统至少保留一个管理员；删除/注销不可恢复且保留最小历史身份；角色或门店变化不迁移历史并撤销全部旧会话。进入第 3 步用户名与管理员重置密码评审。
- M02 第 3 步收到个人资料补充：用户应能在本人详情页编辑姓名、性别、年龄和入职时间。为避免年龄逐年失真，评审稿建议保存出生日期并自动计算只读年龄；新增 API-019 本人资料更新接口，姓名/性别/出生日期/入职日期可改，用户名/角色/门店/密码不可通过该接口修改，等待用户确认。
- 用户明确选择直接保存手工填写的年龄，不保存出生日期、不自动计算或随时间递增；M02 个人资料、DTO、页面和验收草案已改为 `age` 手工字段，并在领域术语中定义为人工维护的当前周岁。
- M02 第 3 步“用户名、个人资料、管理员重置密码与会话失效”于 2026-09-13 获得用户明确确认：用户名小写规范化且不可修改/复用；两类用户可编辑本人姓名、性别、手工年龄和入职日期；本人不能改角色、门店或密码；仅管理员设置/重置密码，重置使目标全部会话失效。进入第 4 步门店生命周期评审。
- M02 第 4 步收到补充：门店必须支持物理删除。为保护历史记录，当前草案将停用与物理删除拆分：仅已停用、无未删除用户绑定且无任何主数据或业务数据引用的空门店可删除；有引用的门店只能停用，禁止级联清理。该边界仍等待用户确认。
- M02 第 4 步“门店生命周期、停用/删除条件与历史数据”于 2026-09-13 获得用户明确确认：门店使用 `ACTIVE/DISABLED`；有关联普通用户时不能停用；仅已停用且无账号或业务引用的空门店可物理删除；存在历史引用时只能停用且禁止级联清理。进入第 5 步门店字段评审。
- M02 第 5 步“门店字段、唯一性与编辑规则”于 2026-09-13 获得用户明确确认：编码规范化后在现存门店中唯一且不可修改，空门店删除后可复用但新建取得新内部 ID；名称允许重名；地址、联系人、联系电话采用已列长度和空白归一规则。进入第 6 步接口与并发评审。
- M02 第 6 步草案统一了响应和并发语义：创建返回 201、更新返回 200、删除/重置密码返回 204；分页使用 `PageResult`；PUT 在请求体携带 `expectedVersion`，DELETE 使用 `If-Match`；删除网络重试可由删除记录判定为同一成功操作并幂等返回 204。
- M02 第 6 步“API-013～026、DTO、分页与并发”于 2026-09-13 获得用户明确确认；接口成功状态、分页结构、新门店默认启用、乐观锁载体和删除安全重试语义均确认。进入第 7 步 seam、权限、审计与安全评审。
- M02 第 7 步草案已移除遗留的用户状态用例，并将会话撤销、跨模块门店引用检查和审计发布拆为稳定端口；门店引用检查不完整时删除安全失败，HTTP 控制器不得编排领域不变量。安全边界复用 M01 会话与 CSRF，并固定本人资料目标来自认证主体。
- 用户最终确认新的全局当前值规则，整体取代此前“按来源日期防回退”和“管理员可手工改价”的结论：当前进价、门店售价、库存只由 POP 商品资料或每日销售汇总导入更新；跨批次以后成功提交者直接覆盖，不比较资料日期或营业日期；同批次按原 Excel 行号取最后一个非空值并给出非阻断提示；日期仅用于追溯，任何角色均不能手工修改这三类当前值。
- M04 第 8 步“两类导入业务预检规则”于 2026-09-13 获得用户明确确认；商品资料非空覆盖、每日销售零销量过滤、重复当前值取最后非空行、跨批次按成功提交顺序覆盖及日期只追溯等规则正式确认。进入第 9 步幂等、并发、原子提交、错误下载与安全评审。
- M04 第 9 步“幂等、并发、原子提交、错误下载与安全”于 2026-09-13 获得用户明确确认；文件指纹、预检过期、门店锁、事务回滚、API-075 `If-Match`、错误文件脱敏与公式注入防护等规则正式确认。进入最终第 10 步页面、任务、验收和完成门禁评审。
- M04 第 10 步“页面、实施任务、验收、门禁与完成定义”于 2026-09-13 获得用户明确确认；10 个评审步骤现已全部通过，M04 可定稿为 `APPROVED`，P0-M04 可标记为 `complete`。G1/G5 数值精度、容量、保留期限和存储方式仍是编码前门禁，不因模块规划通过而自动确认。
- M01 第 8 步于 2026-09-13 获得用户明确确认；8 个步骤全部完成，`docs/api/01-system-auth.md` 已正式定稿为 `APPROVED`，P0-M01 状态为 `complete`。
- M01 最终跨文档口径固定为 Redis 服务端会话 + `SA_SESSION` HttpOnly Cookie + CSRF/Origin 校验，不使用 Bearer/JWT 或浏览器 Web Storage 保存认证凭据；健康检查只把 Java 后端、MySQL、Redis 作为核心项。
